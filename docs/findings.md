# Findings about the docs site (for the documentation team)

This file is a register. Each finding has 1 row: an ID, the date it was raised, a status, the
statement, and the URL that should have answered the question. Statuses:

- **Open** — the docs still do not answer it.
- **Resolved** — the docs now answer it, or the sandbox settled it. The row says how and when.
- **Positive** — the docs were right and the sandbox agreed.
- **Withdrawn** — the entry was a design question for this plug, not a docs gap.

Scrubbed 2026-09-17 against the docs mirror fetched 2026-09-17 from the live site
(`abdm-docs-mirror/MANIFEST.json`, 382 pages); the previous scrub was 2026-09-15. Rows A10–A12, B19
and E15 were added on 2026-09-19 after the site republished (450 pages, catalogue 2026.09.16); the
older rows keep their pre-2026-09-16 page names, which still serve at their old URLs (A12). Stale rows were
corrected, one duplicate was merged, and section J citations were re-checked against the host repos.
The pre-scrub file is kept in the session archive, not in the repo.
Base URL for every source: `https://abdm-docs.dev.eka.care/docs/hiecm/v3/`.

## A. Site and tooling

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| A1 | 2026-09-09 | Resolved 2026-09-15 | `/getting-started/index.md` and `build-with-ai/index.md` rendered MDX components as raw JSX (`<SandboxAction>`). The live pages now render plain links. | `getting-started`, `getting-started/build-with-ai` |
| A2 | 2026-09-09 | Open | `<url>.md` returns 404; only `<url>/index.md` works. `llms.txt` still says "add `.md` to any link". | `/llms.txt` |
| A3 | 2026-09-09 | Open | `/reference/hiecm-*` pages are JS-only. MCP `get_operation` covers them. | `/reference/hiecm-m1` |
| A4 | 2026-09-09 | Open | MCP `get_operation gateway_sessions_create` returns unresolved `$ref`s. Operation ids differ between MCP (`gateway_sessions_create`) and URLs (`gateway-sessions-create`). | MCP `get_operation` |
| A5 | 2026-09-09 | Open | `catalogue_info`: 311 of 319 atoms are `unverified`. | MCP `catalogue_info` |
| A6 | 2026-09-10 | Open | `/api/m2/apis` does not exist. M1 has an `apis` conventions page; M2 does not. | `api/m2` |
| A7 | 2026-09-14 | Resolved 2026-09-17 (docs) | The docs gave no scope words ("instance level", "facility level") for configuration fields, so a multi-facility HMIS had to derive each scope from the shape of the call. `concepts/how-it-fits#one-bridge-many-facilities` now carries a field-by-field table: client id and secret and the bridge callback URL are "your integration, one"; the facility ID is "each facility"; `hipId`, `hipName`, `hipType` are "each facility, once per bridge it links to". It adds that a callback URL or client secret kept in a facility's settings "is a design error". | `concepts/how-it-fits#one-bridge-many-facilities`, `getting-started/sandbox` |
| A8 | 2026-09-15 | Positive | `build-it-well` documents the "validate before you send" table (ABHA number `NN-NNNN-NNNN-NNNN` with dashes, ABHA address, mobile, Aadhaar, `REQUEST-ID`, `TIMESTAMP`), the format-vs-not-found split (`400 {"loginId": "LoginId is invalid"}` vs `404 ABDM-1114 User not found`) and links to `api/m1/errors` for the error shapes. These match the plug's 2026-09-11 sandbox findings. The "Invalid LoginId" (padding) vs "LoginId is invalid" (format) contrast was on `api/m1/errors`, not on this page, and that page withdrew it on 2026-09-17: `Invalid LoginId` from `/v3/enrollment/request/otp` "carries no diagnostic value" (see C2). | `getting-started/build-it-well`, `api/m1/errors` |
| A9 | 2026-09-15 | Positive | MCP `get_fhir_example`, `get_fhir_profile` and `validate_fhir` were enough to build 3 record types. `validate_fhir` returned 0 findings for the plug's OPConsultation, Prescription and HealthDocumentRecord bundles. | MCP `validate_fhir` |
| A10 | 2026-09-19 | Open | The 2026-09-16 `reference/error-codes` page lists 20 codes (M1, P1, P2) with HTTP / Message / Returned by columns. The 2026-09-15 page listed 818 codes with a "What to do" action column. `build-it-well` still says "Every code in the error code reference carries an action. Key your handling to that column". The 2 pages now contradict each other, and an integrator who built the ADR-012 classifier on the action column has no source to rebuild it from. The plug keeps the 818-code catalogue it generated on 2026-09-17 and `scripts/generate-error-catalogue.py` refuses a page without the column. The per-module `errors` pages for M2 and M3 say "records no error code yet". | `reference/error-codes`, `getting-started/build-it-well`, `api/m2/errors`, `api/m3/errors` |
| A11 | 2026-09-19 | Open | The 2026-09-16 skills withdrew `references/test.md` (the certification test matrix) from every skill and withdrew the `abdm-phr-services` skill, without an entry in `whats-new`. The plug cited `abdm-m2/references/test.md` for the certification case ids (`HIP_INIT_GRANT_CONSENT_`, …); those ids now have no source. `whats-new` says a withdrawn artefact qualifies for an entry. | `/skills/index.json`, `whats-new` |
| A12 | 2026-09-19 | Open | The endpoint pages moved to `api/<module>/endpoints/<flow>/<nn>-<method>-<path>`; the old flat pages still serve but left the sitemap. MCP `get_operation` returns a `doc_path` in a third form (`/docs/hiecm/v3/api/m3/endpoints/m3-post-consent-v3-request-init`) that is in neither the sitemap nor the old tree. The `whats-new` pages before 2026-09-15 also left the sitemap while they still serve. | MCP `get_operation`, `sitemap.xml` |

## B. Gateway and bridge

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| B1 | 2026-09-09 | Positive | The session call built from the endpoint page worked first time (HTTP 200); response keys matched the example. | `api/gateway/endpoints/gateway-sessions-create` |
| B2 | 2026-09-09 | Open | The sessions example shows `expiresIn: 0` and `refreshExpiresIn: 0`. The sandbox returns 1200 (20 minutes). `concepts/gateway` prints a response "from the M4 document" with `expiresIn: 1200` and `refreshExpiresIn: 1800`; the endpoint page keeps the zeros. | `api/gateway/endpoints/gateway-sessions-create`, `concepts/gateway` |
| B3 | 2026-09-14 | Open | No endpoint refreshes the gateway session token. The response carries `refreshToken` and `refreshExpiresIn`, and `api/gateway/errors` lists `ABDM-1919 Invalid Refresh token`, but no call consumes them. `m1-token-refresh` is the ABHA user token. The plug creates a new session on expiry. | `gateway-sessions-create`, `api/gateway/errors`, `api/m1/endpoints/m1-token-refresh` |
| B4 | 2026-09-09 | Open | `gateway-update-bridge-url` documents `200 {"message"}`. The sandbox returned **HTTP 202 with an empty body** for a valid PATCH. | `api/gateway/endpoints/gateway-update-bridge-url` |
| B5 | 2026-09-09 | Resolved 2026-09-17 (sandbox) | Whether the bridge `url` may carry a path prefix is unstated. It may: the gateway stored `https://care-abdm-sbx.rithviknishad.dev/api/abdm` and posted the first real callback to `https://care-abdm-sbx.rithviknishad.dev/api/abdm/api/v3/hip/token/on-generate-token` (2026-09-17 07:02:47Z, row in `AbdmCallback`). The page should say the URL may carry a path and that the callback path is appended to it. | `gateway-update-bridge-url` |
| B6 | 2026-09-14 | Positive | The bridge URL is 1 per integrator, never per facility. The 2026-09-15 update says it in words: "One URL covers your whole integration ... It belongs to your bridge, never to a facility". The plug's earlier per-facility bridge URL input was a design over-reach, since removed. | `getting-started/sandbox` |
| B7 | 2026-09-10 | Resolved 2026-09-14 (sandbox) | `gateway-register-bridge-services` names 2 hosts: the prose says `https://apihspsbx.abdm.gov.in`, the curl uses `https://dev.abdm.gov.in`. A third host, `https://facilitysbx.abdm.gov.in`, is on `m1-register-hrp-services`, an M1 page for the same `MutipleHRPAddUpdateServices` call under a different path prefix (`/v1/bridges/...` vs `/v4/int/v1/bridges/...`). Observed: the gateway host answers HTTP 503 "Please make a valid request."; `apihspsbx` answers HTTP 200. The prose host is right; the curl is wrong. | `api/gateway/endpoints/gateway-register-bridge-services`, `api/m1/endpoints/m1-register-hrp-services` |
| B8 | 2026-09-10 | Open | `gateway-register-bridge-services` lists no `REQUEST-ID`, `TIMESTAMP` or `X-CM-ID` headers. Other gateway pages require them. The plug sends them. | same |
| B9 | 2026-09-14 | Open | The HSP Registry reports a failure with **HTTP 200** and an error envelope: `[{"error": {"code": "2500", "message": "Provided facility name is not matched with registered name"}}]`. The page does not warn about this. Error `2500` is absent from the error pages and collides with the documented `ABDM-2500` meanings. | same; `reference/error-codes` |
| B10 | 2026-09-14 | Resolved in part 2026-09-17 (docs) | The HSP Registry uses a 2nd error envelope: `{"code": "HIS-400", "message", "details": [{"code": "HIS-1070", "message": "Bridge ID cannot be blank", "attribute": {"key", "value"}}]}`. `api/m1/errors` now documents it as "Shape 6: the registry error, with the real code nested" and says to read `details[0].code` before the top-level code. `reference/error-codes` lists the `HIS-` code families but shows no envelope, and the gateway pages that hit the registry do not point at Shape 6. | `api/m1/errors`, `reference/error-codes` |
| B11 | 2026-09-14 | Open | `gateway-list-bridge-services` gives `https://dev.abdm.gov.in` in its curl and no prose host, while the registration call it pairs with is on `apihspsbx.abdm.gov.in` (B7). Both hosts answer HTTP 200 for the list. | `api/gateway/endpoints/gateway-list-bridge-services` |
| B12 | 2026-09-14 | Open | The docs do not say how a bridge id is issued. `gateway-list-bridge-services` returns `bridge.id` for an existing bridge; HRP registration needs it as `HRP.bridgeId`. | same |
| B13 | 2026-09-14 | Open | The docs do not say how a user learns a `serviceId`. `gateway-get-bridge-service-by-id` needs it as a path parameter and returns it. The list page shows the services array as a placeholder (`"services": ["<SERVICES>"]`). B18 records the row the sandbox returns. | `api/gateway/endpoints/gateway-get-bridge-service-by-id`, `gateway-list-bridge-services` |
| B14 | 2026-09-10 | Resolved 2026-09-15 (sandbox) | `gateway-get-gateway-certs` shows a placeholder JWKS and lists no bearer token. Observed: **401 without `Authorization`, 200 with the gateway session token.** The set holds 2 RSA keys, `alg` RS256 (kid `AlRb5W…`) and RS512 (kid `oc-l6O…`), each with `x5c`, `x5t`, `x5t2`, `use`. The RS256 kid signs the gateway's own session tokens and, observed 2026-09-17, the token on every callback (E2); the RS512 kid signs the link token (F2). `gateway-get-oidc-config` also needs the token and returns `jwks_uri` = the certs URL. The page should state the bearer requirement and show a captured key. | `api/gateway/endpoints/gateway-get-gateway-certs`, `gateway-get-oidc-config` |
| B15 | 2026-09-14 | Open | HFR "Search facility" by `facilityId` is listed in the HFR search table, but the section opens with "Paths are not yet published", so an HMIS cannot prefill the registered facility name. The user must type the exact HFR name. | `api/m4/undocumented#hfr-search-and-master-data` |
| B16 | 2026-09-14 | Open | The docs do not say whether `facilityName` or `hipName` may contain spaces. The bridge-linkage table allows `-_.(),/` in `facilityName` and "no special characters" in `hipName`; the HFR facility `name` field on the same page says "Alphanumeric, one space between words", the only place a space is mentioned. The plug allows spaces in both. | `api/m4/undocumented` |
| B18 | 2026-09-15 | Open | The real `gateway-list-bridge-services` service row is `{"id": "IN1410000232_1", "name": "FACILITY WITH P", "types": ["HIP", "HIU"], "active": true}`. The docs give `serviceId`, `isHip`, `isHiu`, `registerTime` (on `gateway-get-bridge-service-by-id`) and a placeholder list. Three observations for the docs: the service id is `<HFR facility ID>_<n>`; a service registered with `type: "HIP"` came back with both `HIP` and `HIU`; and **that service id is the HIP ID** the gateway routes on (E1). NHA's own file for the registration call (MCP atom `hiecm.endpoint.m1-register-hrp-services`) shows `HRP[].hipId`, `hipType` and `facilityName` fields that `gateway-register-bridge-services` omits; the `_<n>` suffix is what the registry assigns when `hipId` is not sent. Whether sending `hipId` makes the service id equal the HFR id is untested. | `api/gateway/endpoints/gateway-list-bridge-services`, `gateway-get-bridge-service-by-id`, `gateway-register-bridge-services`, `api/m1/endpoints/m1-register-hrp-services` |
| B17 | 2026-09-15 | Positive | M4 is optional: "a product that registers its facilities [on the NHPR portal] by hand never builds M4". M2 still needs a facility ID and a linked HIP bridge. This settles the plug's ADR-007 "workaround" as the sanctioned route. | `milestones/m4` |
| B19 | 2026-09-19 | Resolved 2026-09-19 (docs) | The certs call now declares the bearer token and `X-CM-ID` (`concepts/callback-authenticity`, "You need an access token for this"), which settles B14. The gateway host table shrank to 2 hosts (`dev.abdm.gov.in`, `apis.abdm.gov.in`); `apissbx` and `live` are withdrawn (`concepts/gateway`). The M4 registry calls now carry their own token from `POST /getManagementToken` and no longer share the gateway session. | `concepts/callback-authenticity`, `concepts/gateway` |

## C. M1 — ABHA identity

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| C1 | 2026-09-09 | Resolved in part 2026-09-17 (docs) | `m1-get-public-certificate` documents `GET /profile/public/certificate` on `https://abhasbx.abdm.gov.in/abha/api`, which returns **404** on the sandbox. The working path is `/v3/profile/public/certificate`, answering `{"publicKey": "<base64 DER SPKI, RSA-4096>"}`: base64 DER without PEM armour. `api/m4/undocumented` now says so in passing ("Where it arrives as bare base64 rather than beginning `-----BEGIN PUBLIC KEY-----`, add the PEM armour yourself ... as the ABHA certificate call requires"). The M1 endpoint page still shows the 404 path, and `concepts/encryption` and `api/m1/apis` still say the URL, headers and response are "not yet published". | `api/m1/endpoints/m1-get-public-certificate`, `concepts/encryption`, `api/m1/apis`, `api/m4/undocumented` |
| C2 | 2026-09-09 | Resolved in part 2026-09-17 (docs) | RSA padding. `concepts/encryption` still says "confirm ... which padding". Since 2026-09-17 `api/m4/undocumented` states "M1 uses RSA-OAEP with SHA-1 under the ABHA certificate", and `api/m1/errors` says "read `encryptionAlgorithm` from the certificate response"; that field is only in the PHR certificate schema (`p1-certificate`: `RSA/ECB/OAEPWithSHA-1AndMGF1Padding`), not in `m1-get-public-certificate`. Sandbox 2026-09-09: OAEP-SHA1 reached UIDAI; PKCS1v15 and OAEP-SHA256 returned `400 {"loginId": "Invalid LoginId"}`. `api/m1/errors` now warns that the enrolment OTP endpoint returns `Invalid LoginId` for wrong and right padding alike and says to test padding on `login/request/otp` with a registered mobile number instead. State OAEP/SHA-1 + base64 on `concepts/encryption` and on every `<RSA_ENCRYPTED_*>` placeholder. | `concepts/encryption`, `api/m4/undocumented`, `api/m1/errors`, `api/p1/endpoints/p1-certificate` |
| C3 | 2026-09-09 | Resolved 2026-09-15 | ABHA service error shapes (`422 {"error": {...}}` vs flat `400 {"<field>": "<msg>"}`) were undocumented. `api/m1/errors` documents them: four shapes on 2026-09-15, six since 2026-09-17 (adding the empty 401 body and the nested `HIS-` registry error, B10). `build-it-well` still says "four error shapes". | `api/m1/errors`, `getting-started/build-it-well` |
| C4 | 2026-09-09 | Open | `m1-encrypt-value`: the page lists the request body as `data` (string, required) and says the 200 body is not described. Sandbox 2026-09-09: `{"encryptedData": "<base64>"}` was accepted and the reply was `{"encryptedData"}`. The documented field name and the observed one differ; not re-run since. | `api/m1/endpoints/m1-encrypt-value` |
| C5 | 2026-09-09 | Open | `X-CM-ID` is documented for gateway calls; neither `api/m1/apis` nor any M1 endpoint page lists it. ABHA-service calls work without it. Not tested whether sending it hurts. | `api/m1/apis` |
| C6 | 2026-09-09 | Open | `m1-profile-get-abha-card` documents no 200 body or content type ("Send the call with Try it"). The plug passes the upstream `Content-Type` through. | `api/m1/endpoints/m1-profile-get-abha-card` |
| C14 | 2026-09-15 | Open | After a mobile-OTP login on 2026-09-15 14:18 the stored transaction held an X-token but **no refresh token**, so the ABHA card could not be fetched after the X-token expired at 14:22 (30 min, as the M1 page now states). Either the sandbox `select-account` response omitted `refreshToken` (the page shows it) or a later refresh failed and the plug dropped it (C8). The plug now logs a token response that lacks `refreshToken`; the next login settles it. | `api/m1/endpoints/m1-login-select-account` |
| C7 | 2026-09-09 | Open | `m1-login-verify`. The intro says an identifier that maps to more than one ABHA gets "the list instead of a token"; the one 200 schema shows `token`, `expiresIn: 1800`, `refreshToken` and `accounts[]` together, so whether a single-account verify (X-token directly) also carries `accounts[]` is unstated. Sandbox 2026-09-09, mobile login: the returned `token` is a T-token that `m1-login-select-account` consumed with a `Bearer` prefix. Since 2026-09-17 the page's `T-token` line says "Like X-token, the value carries a `Bearer ` prefix" while its `X-token` line says "Send the bare token ... adding one is refused as `ABDM-1094`", so the T-token prefix rule is stated by comparison to a header that no longer has one. The plug decides X- vs T-token from the body fields, not the hint. | `api/m1/endpoints/m1-login-verify`, `m1-login-select-account`, `reference/authentication` |
| C8 | 2026-09-10 | Open | `m1-token-refresh`: on 2026-09-15 the curl sent `R-token` bare while a header table said "prefixed", and the endpoint page then settled on a bare value ("The refresh token"). Reopened 2026-09-17: `reference/authentication` still says `R-token` carries a `Bearer ` prefix "like X-token", and the same table now says `X-token` is sent bare (a `Bearer ` prefix is refused as `ABDM-1094 X-token expired`), reversing the 2026-09-15 rule. Not yet run against the sandbox; the plug sends the bare form first and falls back to the prefixed form. | `api/m1/endpoints/m1-token-refresh`, `reference/authentication` |
| C9 | 2026-09-10 | Resolved 2026-09-15 | `loginHint: aadhaar` had no request example and `otpSystem` showed the placeholder `"abdm, aadhaar"`. The page now states the plaintext shape per hint (12 digits for Aadhaar). The `otpSystem` placeholder remains on `m1-phr-request-otp`. | `api/m1/endpoints/m1-login-request-otp`, `m1-phr-request-otp` |
| C10 | 2026-09-10 | Open | Merged into C7. | `api/m1/endpoints/m1-login-verify` |
| C11 | 2026-09-11 | Resolved 2026-09-15 | An ABHA number sent as 14 bare digits fails with `400 {"loginId": "LoginId is invalid"}`; `NN-NNNN-NNNN-NNNN` is accepted. `build-it-well` and `m1-login-request-otp` now state the dashed shape. | `getting-started/build-it-well` |
| C12 | 2026-09-15 | Positive | The M1 page was rewritten after NHA's review of 11 September 2026: Aadhaar OTP is the only mandatory creation route for a private integrator; face and biometric are optional; demographic auth and child ABHA are not for private integrators; enrol-by-document is "not recommended". Login token lifetimes are now stated (X-token 1800 s, R-token 1296000 s). | `milestones/m1` |
| C13 | 2026-09-09 | Withdrawn | "No identifier-system URI is prescribed for ABHA number / ABHA address as FHIR identifiers." The plug chose its own systems (`abdm/abha-number`, `abdm/abha-address`, ADR-004). The NRCES Patient profile is the place to look; not an HIE-CM docs gap. | — |
| C14 | 2026-09-17 | Open | `ABDM-1114` has 2 meanings. The M1 error table reads "The provided name does not match the name in DigiLocker records", but every login path answers `404 ABDM-1114` with a not-found message for an identifier that has no ABHA account: `User not found` (2026-09-11, ABHA number) and `No ABHA user registered with this Aadhaar number.` (2026-09-17, Aadhaar login verify). `build-it-well` states the second meaning. The plug trusts the login context and offers the create journey on this code. | `api/m1/errors`, `getting-started/build-it-well` |

## D. M1 — Scan and Share

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| D1 | 2026-09-14 | Open | The counter QR code content is not published. The M1 page says only "a URL with two parameters: your HIP id and a counter context such as `OPD1`", and with `concepts/phr` that the context is up to 20 alphanumeric characters and never the facility id, HPID, HIP id or HIP name. No host, path or parameter names. The plug reads the format from `ABDM_SHARE_QR_URL_TEMPLATE`. | `milestones/m1#a-patient-shares-their-profile-at-your-counter`, `concepts/phr#scan-and-share-at-a-facility` |
| D2 | 2026-09-14 | Open | `m1-on-share-acknowledgement` has 2 bodies. The M1 page: `{acknowledgement: {status, abhaAddress, profile: {context, tokenNumber}, error}}`, prose host `dev.abdm.gov.in/api/hiecm`, curl host `abhasbx.abdm.gov.in/abha/api`. The PHR page `p2-onpatientshare` adds `profile.expiry` and `response.requestId` and uses `phrsbx.abdm.gov.in`. The `m1-receive-patient-share` curl shows `abhasbx.../patient-share/v3/share`, which is the HIP's own bridge path. The plug sends the union on the gateway host. | `api/m1/endpoints/m1-on-share-acknowledgement`, `m1-receive-patient-share`, `api/p2/endpoints/p2-onpatientshare` |
| D3 | 2026-09-14 | Open | The token number format is not specified (`m1-on-share-acknowledgement` shows `TKN-0042`). `concepts/phr` gives two time limits "we have tested neither": a second token is blocked for 60 minutes, and the token is valid for the next 30 minutes and configurable. The M1 page adds that the patient's app waits 30 seconds for the acknowledgement. The plug uses `<context>-<nnn>` per counter per day and sends `expiry: "1800"`, a field only the PHR-side page carries. | `concepts/phr#scan-and-share-at-a-facility`, `milestones/m1#a-patient-shares-their-profile-at-your-counter`, `api/m1/endpoints/m1-on-share-acknowledgement` |
| D4 | 2026-09-14 | Open | The callback signature for `patient-share/v3/share` is described only as `Authorization: <bearer>`. The plug verifies it with the gateway JWKS like every other callback. No real gateway callback observed yet. | `api/m1/endpoints/m1-receive-patient-share` |

## E. M2 — headers, paths, callbacks

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| E1 | 2026-09-10 | Resolved 2026-09-17 (sandbox), docs wrong | `X-HIP-ID` is **not** the HFR facility ID on the sandbox, whatever the docs and the docs team (2026-09-14) say. It is the service id the registry issued at HRP registration, `IN1410000232_1` (B18). Observed: `m2-generate-link-token` sent with `X-HIP-ID: IN1410000232` is accepted with 202 and **its callback never arrives** (6 such requests on 2026-09-15 and 2026-09-17, waits up to 60 minutes); the same call with `X-HIP-ID: IN1410000232_1` produced the callback in 1.9 s (2026-09-17 07:02:47Z), and the link token it carried names `"hipId": "IN1410000232_1"` in its claims. The pages should say: send the service id from `gateway-list-bridge-services`, and a wrong HIP ID is accepted silently. The plug now reads the HIP ID off the gateway's service list and refuses to link until it exists. | `reference/authentication`, `api/m2/endpoints/m2-generate-link-token`, `concepts/how-it-fits#one-bridge-many-facilities` |
| E2 | 2026-09-10 | Resolved 2026-09-17 (sandbox) | Callback authenticity: the signed token travels in **`Authorization: Bearer <JWT>`**. Observed on the first real callback: RS256, `kid` = the RS256 key in the gateway JWKS (B14), `iss` `https://dev.abdm.gov.in/auth/realms/central-registry`, `typ` `Bearer`, `azp` and `clientId` `gateway`, `preferred_username` `service-account-gateway`, `aud` `account`, 20-minute `exp`. It is the gateway's own session token, not a signature over the body: verifying it proves the sender, nothing binds it to the payload. Other headers on the callback: `REQUEST-ID` (fresh UUID), `TIMESTAMP` (UTC ms `Z`), `X-HIP-ID`, `Content-Type: application/json`, `User-Agent: ReactorNetty/1.1.19`, `Traceparent`/`Tracestate`, `X-Amzn-Trace-Id`; source IP `13.203.243.253`. The page should name the header and say the token is a client-credentials JWT. | `concepts/callback-authenticity` |
| E3 | 2026-09-10 | Resolved in part 2026-09-17 (sandbox) | Callback paths. **The real path carries an `/api` prefix the pages omit**: the gateway posted to `{bridgeUrl}/api/v3/hip/token/on-generate-token` while `m2-on-generate-token-result` names `POST /v3/hip/token/on-generate-token`; the page's own curl (`https://dev.abdm.gov.in/api/v3/hip/token/on-generate-token`) has the right relative path. The 3 other result pages (`on_carecontext`, `links/context/on-notify`, `patients/sms/on-notify`) follow the same pattern and are presumed `/api/v3/...`. The inbound pages already spell `/api/v3/hip/...`; the `/v0.5/...` spellings in prose (discover, link init/confirm, consents, health-information) remain unobserved. The plug maps every spelling and stores unknown paths. | `api/m2/endpoints/m2-on-generate-token-result`, `m2-on-carecontext-result`, `m2-on-context-notify-result`, `m2-on-sms-notify-result`, `m2-on-discovery-request`, `m2-on-link-init`, `m2-on-link-confirm`, `m2-on-health-information-request`, `m2-consent-hip-on-notify` |
| E4 | 2026-09-10 | Open | Callback curl examples render bridge-relative paths on the ABDM host, e.g. `https://dev.abdm.gov.in/api/api/v3/hip/...`. | `api/m2/endpoints/m2-on-health-information-request` |
| E5 | 2026-09-10 | Open | `m2-hip-health-information-on-request`, `m2-hip-data-flow-notify` and `m2-consent-hip-on-notify` list only `Authorization`. Other M2 pages require `REQUEST-ID`, `TIMESTAMP`, `X-CM-ID`. The plug sends them. | those 3 pages |
| E6 | 2026-09-10 | Open | Retry count, backoff and timeout for inbound discovery, link init and link confirm are "not stated in NHA's material". No page gives a callback timeout for the result legs either. | `m2-on-discovery-request`, `m2-on-link-init`, `m2-on-link-confirm` |
| E7 | 2026-09-10 | Open | Ack body for inbound callbacks: the pages say "202" or "200" and "a 202 carrying the wrong body is still a failure", but do not say what the right body is. The plug returns `202 {}`. | same |
| E8 | 2026-09-15 | Open | `milestones/m2` says "Four callbacks name a path and carry no documented payload: discovery, link init, link confirm, and consent notify. Do not assume a body." The endpoint pages `m2-on-discovery-request`, `m2-on-link-init` and `m2-on-link-confirm` do list body fields. The 2 statements contradict each other. | `milestones/m2`, the 3 pages |
| E10 | 2026-09-15 | **Answered 2026-09-17** | Retry after a failed delivery is undocumented ("Whether and when delivery attempts stop is not documented yet", troubleshooting). Measured: the gateway repeats a refused delivery **about 16 minutes later** (965 s and 963 s after the callback `TIMESTAMP`) and sends **the same bearer token**, which by then has expired. The token is a 20-minute (1200 s) session token minted up to 9 minutes before the callback is built, so the repeat always carries a dead token and a strict `exp` check throws a genuine answer away. Observed twice: 07:31:50Z carried a valid link token (bearer `exp` 07:26:27Z) and 10:16:16Z carried `ABDM-1027` (bearer `exp` 10:14:26Z). Both were refused 401 and lost, which left the desk on a spinner. The plug now accepts a late token for `ABDM_CALLBACK_SIGNATURE_LEEWAY_SECONDS` and records how late it was (ADR-012 D4). The page should state the repeat interval and that the token is not re-minted. | `troubleshooting/callback-never-arrives` |
| E11 | 2026-09-17 | **Resolved 2026-09-18 (sandbox); the cause was ours and the docs page is wrong** | `m2-hip-link-care-context` refuses a request with **HTTP 400 and an empty body** (no content type, no code) when `hiType` is an **array**. It needs a **string**. The page types the field `object[]` and its own example sends `"hiType": ["Prescription"]`, so the page is wrong. Measured 2026-09-18 with 1 variable changed at a time, same token, same headers, same body: `hiType: ["DischargeSummary"]` answers the empty 400; `hiType: "DischargeSummary"` reaches the business logic and answers **202**, then `on_carecontext` answers `Successfully Linked care context`. A string that joins 2 types with a comma is refused in JSON: `Invalid HIType, it must be in Prescription,DiagnosticReport,OPConsultation,DischargeSummary,ImmunizationRecord,HealthDocumentRecord,WellnessRecord,Invoice`. To carry several HI types, send 1 `patient` block for each type in the same call, with the same `careContexts`; that answers 202 and the gateway sends 1 callback for the call (F7). The empty 400 is therefore a shape error the service does not name. **Order of the checks, measured 2026-09-17 12:34Z to 12:40Z.** A missing header answers an empty 401. A garbage `X-Link-Token` answers `ABDM-9999: Invalid X Auth token`. A body with `patient: []` answers `ABDM-9999: patient attribute required in the payload`. `X-HIP-ID` is not read before the shape check. Past the shape check, a bad field answers the empty 400 with no code. Every refusal comes from `server: istio-envoy` with `x-envoy-upstream-service-time` 16-25 ms and carries `correlation-id` and `activityid`; the plug keeps these in `AbdmOutboundRequest.response_headers`. **2 corrections to my earlier reading.** (1) I read the 500 `303001 ... HIECM-HIP-INITIATED-LINKING--vv3_APIproductionEndpoint [ State : SUSPENDED ]` as proof of an ABDM outage. It is not. The API manager suspends an endpoint after a transport failure and the state is transient; outbound rows 433 and 434 show the same code on a healthy API, `HIECM-LinkToken--vv3`, which answered 202 again 3 minutes later. (2) The check order above shows only that our token and our top-level shape pass, not that the whole body is right. The 1 field it does not cover, `hiType`, was the cause. **The page must name a code for this 400 and must type `hiType` as a string.** | `api/m2/endpoints/m2-hip-link-care-context`, `api/m2/errors` |
| E12 | 2026-09-17 | Positive | Callback latency: the gateway posted `on-generate-token` **1.9 s** after the 202 (TIMESTAMP header `07:02:47.219Z`, received `07:02:47.348Z`). The docs give no figure; "a few seconds" would let an integrator tell "not yet" from "never" (E1). | `troubleshooting/callback-never-arrives` |
| E13 | 2026-09-17 | Resolved in part 2026-09-17 (sandbox) | A repeat `m2-generate-link-token` for the same ABHA address and HIP is answered: one sent 25 minutes after an answered one produced its callback in 8.8 s (an error callback, E14). One sent 13 minutes after an answered one was accepted with 202 but never answered and then held open (`ABDM-1092` 3 minutes later); the difference is not documented. | `api/m2/endpoints/m2-generate-link-token`, `concepts/linking` |
| E14 | 2026-09-17 | Open | **`ABDM-1027 You are blocked. Please try again after 24 hours.`** arrived as an error callback on `on-generate-token` 8.8 s after a 202, after about 20 link calls had been refused with the empty 400 (E11) in 12 minutes and 5 token requests in 25 minutes. The code came as `"ABDM-1027: "` with a trailing colon and space, exactly as `build-it-well` warns. The docs list the code under M2 errors with "Blocked, no retry" and no threshold; the trigger (refused link calls, or call rate) and the scope (this client, this HIP, or this ABHA) are not stated. `gateway-list-bridge-services` and the session call kept working. **Scope measured 2026-09-17: the block follows the ABHA address, not the client.** Patient 61 was blocked while patients 76 and 77 were served at the same HIP in the same minutes. | `api/m2/errors`, `concepts/gateway#limits-to-code-against` |
| E9 | 2026-09-15 | Open | `m2-sms-deep-link-notify` lists no `X-HIP-ID` header, but the body carries `notification.hip.id`, and the page does not say whether the body `requestId` must equal the `REQUEST-ID` header. The plug sends the same UUID in both. | `api/m2/endpoints/m2-sms-deep-link-notify` |
| E15 | 2026-09-19 | Positive (docs 2026-09-16) | 4 earlier gaps closed in the republished M2 pages, and the plug's parsers already match them without a code change. (1) Callbacks declare bearer authentication in `Authorization` (E2). (2) The M2 milestone page writes the callback path with the `/api` prefix, `/api/v3/link/on_carecontext` (E3). (3) The 4 HIP-inbound bodies are published on `m2-abdm-user-initiated-linking-hip/03` (link init: `transactionId`, `abhaAddress`, `patient[].careContexts[].referenceNumber`), `/05` (link confirm: `confirmation.token` as an integer, `confirmation.linkRefNumber`), `m2-consent-management-data-flow-hip/01` (consent notify: `notification{status, consentId, consentDetail, signature, grantAcknowledgement}`) and `/03` (health-information request: `transactionId` at the top level, `hiRequest{consent.id, dateRange, dataPushUrl, keyMaterial}`) (E8, H1, H3). (4) `hiTypes` is 8 values with `Invoice` on `concepts/fhir` and in the MCP schema enum (I4). Still open on the same pages: `m2-hip-link-care-context` (`m2-abdm-hip-initiated-linking-hip/01`) types `hiType` as an array (E11), and `/03` (notify) still puts the ABHA address in `careContext.patientReference` (F8). The "Notification to mobile" journey left `milestones/m2` and `concepts/hip-hiu` ("Two routes"), while the SMS endpoint page `m2-abdm-hip-initiated-linking-hip/05-m2-post-hip-v3-link-patient-links-sms-notify2` remains. | `milestones/m2`, `concepts/callback-authenticity`, `concepts/fhir`, the 4 M2 flow pages |

## F. M2 — link token and care contexts

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| F1 | 2026-09-10 | **Open; the "immediately" reading is disproven 2026-09-17** | Link token lifetime conflict. `concepts/linking`, `milestones/m2`, the glossary and `how-it-fits` say "valid six months, stored at registration"; `m2-generate-link-token` says "short-lived ... must be called immediately before the linking call, tokens expire quickly". The JWT `exp` is `iat` + 182.5 days (F2). Token age is **not** the cause of E11: at 15:47:40.032 a token callback arrived and at 15:47:40.128, 96 ms later, the link call went out with a 2-second-old token and got the same empty HTTP 400. The plug's 5-minute freshness rule was therefore removed (ADR-012); it only forced a re-request inside the F6 window and earned `ABDM-1092`. The docs should say the token is valid 6 months, or state what "expire quickly" measures. | `concepts/linking`, `api/m2/endpoints/m2-generate-link-token` |
| F2 | 2026-09-15 | Resolved 2026-09-17 (sandbox) | The link token is a JWT, alg **RS512** (signed by the RS512 key in the gateway JWKS), claims `{hipId, abhaNumber (integer), transactionId, abhaAddress, sub (= abhaAddress), iat, exp}`; `exp - iat` = 15 768 000 s = 182.5 days, which is the "six months" of `concepts/linking`. The page should publish the claim set. | `concepts/linking`, `api/m2/endpoints/m2-on-generate-token-result` |
| F3 | 2026-09-15 | Positive | "A care context is linked when the callback says so, not when the call returns" (whats-new 2026-09-10). The plug keys `linked` on `/v3/link/on_carecontext`. | `whats-new/2026-09-10`, `api/m2/endpoints/m2-on-carecontext-result` |
| F4 | 2026-09-15 | **Answered 2026-09-18 (sandbox)** | `m2-on-carecontext-result` names no success value for the free-text `status`. Measured: a link that succeeds answers `"status": "Successfully Linked care context"` and carries no `error` block. The `on-notify` leg answers `"acknowledgement": {"status": "SUCCESS"}`. The plug reads the absence of an `error` block as success, which both values confirm. The pages should name these values. | `api/m2/endpoints/m2-on-carecontext-result`, `api/m2/errors` |
| F5 | 2026-09-10 | Open | `m2-generate-link-token` types `abhaNumber` as integer; `m2-hip-link-care-context` types it as string. | both pages |
| F6 | 2026-09-15 | **Measured 2026-09-17** | `ABDM-1092 Duplicate Link token request` fires with a **fresh `REQUEST-ID`**, so the docs' action "New request id" does not apply. The key is the ABHA address + HIP, and the window runs from the **accepted** request whether or not that request was answered. Measured over 23 requests: refused at 4, 156, 181, 186, 198, 214, 217, 246, 321, 350 and **362 s**; accepted at **374**, 685, 776, 825, 884, 921, 1277, 8357 and 151488 s. The boundary lies between 362 s and 374 s, so the window is about 6.2 minutes. A delivered callback does not close it: patient 77 got a token at 15:47:40 and was still refused at 15:53:37. The plug now holds `errors.TOKEN_REQUEST_WINDOW` = 7 minutes and never sends inside it (ADR-012). The page should state the window and what "duplicate" means for this call. | `api/m2/errors`, `api/m2/endpoints/m2-generate-link-token` |
| F7 | 2026-09-18 | **Measured 2026-09-18 (sandbox); undocumented** | The docs do not say how to link a care context that holds several HI types, because they show `hiType` as an array (E11). Measured: send 1 `patient` block for each HI type in the same call. Each block repeats the same `referenceNumber`, `display` and `careContexts`, and carries 1 `hiType` string and its own `count`. Care context 17 with 2 HI types answered **202** and reached `linked`. The gateway sends 1 `on_carecontext` callback for the call, not 1 for each block. | `api/m2/endpoints/m2-hip-link-care-context` |
| F8 | 2026-09-18 | **Measured 2026-09-18 (sandbox); the docs example is wrong** | `m2-link-care-context-notify` needs `careContext.patientReference` to hold the **HIP's internal patient reference**, the same value the link call sends as `patient.referenceNumber`. The page's example puts the ABHA address there. Measured: the ABHA address answers `ABDM-1006 No care context linked with given reference number`; the internal reference answers `SUCCESS`. | `api/m2/endpoints/m2-link-care-context-notify` |
| F9 | 2026-09-18 | **Measured 2026-09-18 (sandbox); undocumented** | ABDM needs time to index a new link before it accepts the notify. A notify sent 0.6 s after the `on_carecontext` callback is refused with `ABDM-1006 No links found for the patient in the given HIP`; the same notify 50 s later answers `SUCCESS`. The docs give no delay. The bound measured is more than 0.6 s and less than 51 s. The plug now waits `NOTIFY_DELAY_SECONDS` (30 s) and repeats a refusal with a longer wait. | `api/m2/endpoints/m2-link-care-context-notify` |
| F10 | 2026-09-18 | Open | The docs do not say what a second `m2-hip-link-care-context` for the **same care context reference** with a **new `hiType`** does at ABDM: add the type, or answer `ABDM-1056 already linked` and keep the old set. ADR-013 stages records and links them over time, so a visit gains types after its first link. Until measured, the plug sends every linked + queued type in each call. Measure on the sandbox: link `Prescription`, then `OPConsultation` for the same reference, then run discovery from a PHR app and read the `hiType` list. | `api/m2/endpoints/m2-hip-link-care-context`, `concepts/linking` |

## G. M2 — discovery and user-initiated linking

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| G1 | 2026-09-15 | Open | `patient.verifiedIdentifiers[]` and `unverifiedIdentifiers[]` are typed `object[]` with no element shape. The plug reads `{type, value}` pairs. | `api/m2/endpoints/m2-on-discovery-request` |
| G2 | 2026-09-15 | Open | The docs do not say how to answer discovery when no patient matches, or when several match. The plug sends `patient: []`. `ABDM-1012 no records found against ABHA address` exists in the error table but no page ties it to this reply. | `m2-on-discover-care-contexts`, `api/m2/errors` |
| G3 | 2026-09-10 | Open | The docs do not say how the HIP must create, deliver, store or validate the 6-digit OTP for link init and confirm. Only `confirmation.token` and the `link.meta` fields appear. The plug sends its own OTP over Care's SMS backend and stores a hash. | `m2-receive-link-confirm`, `m2-on-link-confirm` |
| G4 | 2026-09-15 | Open | `m2-receive-link-init` and `m2-receive-link-confirm` list no `error` field. The docs do not say how the HIP reports "no such patient" or "wrong OTP". The plug adds `error {code, message}` like every other callback answer. | those 2 pages |

## H. M2 — consent and data transfer

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| H1 | 2026-09-10 | Resolved in part 2026-09-15 | The HIP consent notification has no M2 endpoint page. `m2-consent-hip-on-notify` is the HIP's acknowledgement (`acknowledgement{status, consentId}`, `response.requestId`) and carries no inbound body. **The body is published** on the M3 page `m3-on-consent-request-notify-hip` (`status`, `consentId`, `consentDetail{...}`, `signature`, `grantAcknowledgement`). The M2 pages do not link to it. | `api/m2/endpoints/m2-consent-hip-on-notify`, `api/m3/endpoints/m3-on-consent-request-notify-hip` |
| H2 | 2026-09-15 | Open | The consent artefact `signature` is "Base64-encoded digital signature of the consent artefact" and the M2 ack page calls it a "digital signature for validation", but no page gives the algorithm, the signed bytes, or the key. The plug stores it and does not verify it. | `api/m3/endpoints/m3-on-consent-request-notify-hip`, `api/m2/endpoints/m2-consent-hip-on-notify` |
| H3 | 2026-09-10 | Resolved in part 2026-09-15 | `m2-on-health-information-request` has no body section. **The body is inferable** from `m3-hiu-health-information-request` (`hiRequest{consent{id}, dateRange{from,to}, dataPushUrl, keyMaterial}`) and `p2-as-record-on-share` (`transactionId`). The HIP page should carry it. | `api/m2/endpoints/m2-on-health-information-request`, `api/m3/endpoints/m3-hiu-health-information-request`, `api/p2/endpoints/p2-as-record-on-share` |
| H4 | 2026-09-15 | Resolved in part 2026-09-15 | The data-push body the HIP must send is not on any M2 page. It is on the M3 receive page `m3-on-health-information-transfer`: `{pageNumber, pageCount, transactionId, entries[{content, media, checksum, careContextReference}], keyMaterial}`. Link it from `concepts/data-flow`. | `api/m3/endpoints/m3-on-health-information-transfer` |
| H5 | 2026-09-15 | Open | Encryption details the docs leave open: the HKDF hash (the plug uses SHA-256) and `info` (empty); the `content` encoding (the plug sends base64 of ciphertext + GCM tag); `keyValue` encoding (the M3 page says "32 bytes"; the plug also accepts DER). "Sign with its long-term private key" has no field in the push body. | `concepts/data-flow`, `m3-on-health-information-transfer` |
| H6 | 2026-09-10 | Open | `concepts/fhir` says an HMIS must implement 8 record types including `Invoice`, and itself notes "Two mismatches": the M3 table omits `Invoice`, which "appears only in the M2 error message". `concepts/consent` lists 7 HI types and says the same. MCP `list_fhir_profiles` returns 7 record profiles plus `DocumentBundle`; `get_fhir_profile Invoice` says not found. The docs say "check the swagger" rather than settling whether `Invoice` is a valid HI type. | `concepts/fhir`, `concepts/consent`, MCP |
| H7 | 2026-09-15 | Open | The docs do not say which records a HIP must include when the consent names a date range: records dated in the range, or records of care contexts whose visit falls in the range. The plug filters by the encounter start date. | `concepts/data-flow` |

## I. FHIR mapping gaps (CARE data → NRCES profiles)

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| I1 | 2026-09-15 | Open | CARE `Condition.category` has no "chief complaint" value. The plug maps `problem_list_item` to the OPConsultRecord "Chief complaints" section and other categories to "Medical history". | `care/emr/resources/condition/spec.py`; MCP `get_fhir_profile OPConsultation` |
| I2 | 2026-09-15 | Open | `HealthDocumentRecord` attachments: the plug inlines file bytes when the object store answers; otherwise it falls back to a signed URL that expires in 1 hour. The docs do not say whether `attachment.url` is acceptable. | `concepts/fhir` |
| I3 | 2026-09-15 | Open | NRCES `Practitioner` examples carry an HPR identifier. CARE users have none. The plug sends the CARE user id as the identifier. The docs do not say whether HPR is required for a valid bundle. | MCP `get_fhir_example` |
| I4 | 2026-09-15 | Open | No CARE model exists for `Immunization`, `Procedure` or wellness vitals grouping. `ImmunizationRecord`, `WellnessRecord`, `DiagnosticReport`, `DischargeSummary` and `Invoice` builders are deferred (ADR-011). | — |

## L. M3 — HIU (consent requests and fetched records)

Raised while building ADR-014 from the 2026-09-16 pages. None of these has met the sandbox yet.

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| L1 | 2026-09-19 | Open | The init page lists `REQUEST-ID`, `TIMESTAMP` and `X-CM-ID` only, while `status`, `fetch` and `health-information/request` list `X-HIU-ID` too and the ack page (`hiu/on-notify`) lists none. The body carries `hiu.id`, so the header may be redundant on init; the docs do not say which calls the gateway routes on the header. The plug sends `X-HIU-ID` on every HIU call. The value is assumed to be the 1 service id the sandbox issued with `types: ["HIP", "HIU"]` (B18); no page says how an HIU id is issued or whether it equals the HIP id. | `m3-consent-management-data-flow-hiu/01`, `03`, `06`, `07`, `09` |
| L2 | 2026-09-19 | Open | `permission.accessMode` (`VIEW`, `STORE`, `STREAM`, `QUERY`) and `permission.frequency` (`unit`, `value`, `repeats`) are enumerated and typed, and never explained. An HIU that stores the bundle for the consent's validity cannot tell whether that is `VIEW` or `STORE`. The plug sends the example values (`VIEW`; `HOUR`, 1, 0). | `m3 .../01`, `concepts/consent` |
| L3 | 2026-09-19 | Open | `consent.requester.identifier {value, type, system}` is required and shown as `REG1` / `MH1001` / `https://www.sample.com`. No page names the allowed `type` values or says whether `system` is a URL. The plug sends the medical council registration or the Care username with its own type words. | `m3 .../01` |
| L4 | 2026-09-19 | Open | `acknowledgement[].consentId` on `consent/v3/request/hiu/on-notify` is "The consent artefact id", yet the page example acknowledges the notify example's `consentRequestId` value (`e3c74829…`), and a DENIED or EXPIRED notify carries no artefact at all. What to acknowledge a denial with is not said. The plug sends the artefact ids when there are any and the consent request id otherwise. | `m3 .../05`, `06` |
| L5 | 2026-09-19 | Open | `entries[].checksum` is "Md5 checksum of the content before encryption", without saying which bytes: the JSON as serialised by the sender, or a canonical form. Two correct systems can disagree on whitespace and key order. AES-GCM already authenticates the ciphertext, so the plug records a mismatch and keeps the record. | `m2-consent-management-data-flow-hip/05` |
| L6 | 2026-09-19 | Open | The push to `dataPushUrl` says "Authorisation token is mandatory for this API" and not which token. An HIU cannot verify a token it does not know the issuer of. The plug assumes the HIP's gateway session token (what its own M2 side sends) and verifies it against the gateway JWKS; a token from elsewhere is stored with `signature_status=failed` and refused with 401. `concepts/data-flow` also says the HIP signs the payload "with its long term private key", and no page shows where that signature travels. | `m2 .../05`, `concepts/data-flow` |
| L7 | 2026-09-19 | Open | `on-status` enumerates `REQUESTED, DENIED, EXPIRED, REVOKED` and omits `GRANTED`; `notify` enumerates all 5. An HIU that polls cannot learn of a grant from the status call, only from the notify. The plug records whatever arrives. | `m3 .../04`, `05` |
| L8 | 2026-09-19 | Open | `on-fetch` puts `dataEraseAt` and `frequency` beside `permission` inside `consentDetail`; the init page and the HIP notify page put them inside `permission`. The plug reads both places. | `m3 .../08`, `01`, `m2 .../01` |

## J. Care core caveats (for the plug, not for the docs team)

| ID | Raised | Status | Finding | Source |
|---|---|---|---|---|
| J1 | 2026-09-09 | Handled | `PatientIdentifier` rows are invisible until `Patient.instance_identifiers` is rebuilt; only the patient viewset rebuilds it. The plug calls `patient.build_instance_identifiers()` in `abdm/signals.py`. | `care/emr/models/patient.py:108`, `care/emr/resources/patient/spec.py:275`, `care/emr/api/viewsets/patient.py:147,190,438` |
| J2 | 2026-09-09 | Handled | Auto-maintained identifier configs are dropped from create payloads, so the ABHA cannot arrive as an identifier from the form. Hence `extensions.abdm.txn_id` → `post_save`. | `care/emr/api/viewsets/patient.py:138` |
| J3 | 2026-09-14 | Handled | The desk inbox and the ABHA wizard use `can_create_patient`, which is organisation-scoped, not facility-scoped. | `care/security/authorization/patient.py:87` |
| J4 | 2026-09-15 | Handled | `Encounter.save()` runs for cache-only updates (`sync_organization_cache`). The plug's `post_save` skips saves whose `update_fields` touch neither `status`, `encounter_class` nor `period`. | `care/emr/models/encounter.py:39,60-63` |
| J6 | 2026-09-15 | Handled | Care runs every request in a transaction (`ATOMIC_REQUESTS = True`, `config/settings/base.py:73`). An exception in the callback view rolled back the stored callback row, so a real gateway POST on 2026-09-15 17:37 left no evidence (only a 500 in the runserver log). The receiver now catches everything after the insert: 401 for a bad signature, 503 when the JWKS cannot be read, and the row keeps `signature_error`. | `config/settings/base.py:73` |
| J7 | 2026-09-15 | Handled | A client-side navigation between an `/admin/*` route and an app route crashes the host: `AppRouter` swaps the sidebar on the same tree and `PinPageDialog` throws "change in the order of Hooks". The plug's cross-layout links (`Open setup` on `/admin/abdm`, `Open ABDM admin` on the setup page) are plain anchors, so they do a full page load. | `care_fe/src/Routers/AppRouter.tsx:132-138`, `care_fe/src/components/Common/PinPageDialog.tsx` |
| J8 | 2026-09-15 | Handled | A federated remote built by Vite emits `modulepreload` dependency lists that resolve against the host origin and 404 there (about 25 console errors per plug page). `build.modulePreload = false` in the remote's `vite.config.ts` removes them; the reference plug has the same noise. | `frontend/vite.config.ts` |
| J5 | 2026-09-15 | Handled | Care sends a fixed OTP outside production (`"45612"` when `IS_PRODUCTION` is false, `care/emr/api/otp_viewsets/login.py:98`). The plug uses the same rule for the user-initiated link OTP with its own fixed value, `123456` (`backend/src/abdm/hip/discovery.py:38`). | `care/emr/api/otp_viewsets/login.py:98` |

## K. Product decisions recorded here (durable)

- 2026-09-10 — Find by ABHA does not use core's `?identifier=` search. ABDM verifies the ABHA by OTP before Care looks it up.
- 2026-09-10 — Care-context reference number = `Encounter.external_id`; display = `<OPD|IPD|class> records for <day>`; discovery matches on verified ABHA address or number only (ADR-008 decisions 2-4).
- 2026-09-14 — `X-HIP-ID` is derived by the plug, never client-supplied. Superseded 2026-09-17: it is the service id the gateway holds for the facility (E1), read from `gateway-list-bridge-services` and stored on the facility; a facility with no service is not configured for M2.
- 2026-09-15 — The bridge id is read live from the gateway and cached; no snapshot table (ADR-011). The bridge UI is instance-level at `/admin/abdm`, not on a facility page (Rithvik).
- 2026-09-15 — Day-1 record types: OPConsultation, Prescription, HealthDocumentRecord (ADR-011). Superseded 2026-09-18 (ADR-013): OPConsultation (outpatient only), Prescription (`MedicationRequestPrescription`, not draft), DiagnosticReport (final), DischargeSummary (generated PDF); HealthDocumentRecord removed.
- 2026-09-18 — Records are staged, not linked, when a clinician saves them. The desk links selected records on the ABDM tab; a completed or discharged Encounter links every staged record; retries hourly, 3 times (ADR-013).
- 2026-09-19 — M3 as HIU (ADR-014): the UI is the 3rd card of the ABDM Records encounter tab; a fetched bundle is stored and its content erased at the consent `dataEraseAt` or on revoke, deny or expiry; a request goes to every HIP unless the desk picks 1 provider; defaults CAREMGT, 7 clinical types, 12 months, 30 days, editable; the grant runs the fetch, the data request, the decryption and the notify by itself.
