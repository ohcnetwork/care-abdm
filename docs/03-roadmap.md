# 03 — Roadmap

Status legend: [ ] not started · [~] in progress · [x] done (with evidence link)

Order (Rithvik, 2026-09-09): M1 → M2 → M3 → M4. Since 2026-09-15 the docs say the NHPR portal route is sanctioned and "a product that registers its facilities there by hand never builds M4" (`milestones/m4`). The ADR-007 facility setup is therefore the route, not a workaround; Phase 5 shrinks to the API extras. Every phase ends on
an **observed sandbox result**, per the docs' own scaffold loops
(`.agent/skills/abdm-m*/references/scaffold.md`).

## Phase 0 — Knowledge base and environment  [x]
- [x] Mirror docs (`docs/abdm-docs-mirror/`), download the nine skills (`.agent/skills/`).
- [x] Register the docs MCP server in the Hermes profile (`hermes config set mcp_servers.abdm-docs.url …`). Needs a Hermes restart to expose tools.
- [x] Read CARE host contract, write `02-care-host-contract.md`.
- [x] Sandbox `clientId`/`clientSecret` received → `.env.local` (ignored; verified `git check-ignore`).
- [x] Dev callback exposure: cloudflared named tunnel `care-abdm-sbx` → https://care-abdm-sbx.rithviknishad.dev → localhost:8000 (ADR-005). Bridge URL registered 2026-09-09 via `abdm.gateway.bridge.update_bridge_url()` → HTTP 202.
- [x] ADR-004 data model drafted (identifiers + extensions first; Facility has no extension surface — flagged).
- [x] Skeleton repos: `backend/` from `care_token_display`, `frontend/` from `care_token_display_fe`. Evidence 2026-09-10: code exists. `npm run build` produced `dist/assets/remoteEntry.js`. `manage.py check` passed with the plug. Roadmap records `remoteEntry.js` served with CORS `*` on :4174.
- [x] 2026-09-15 Docs mirror rebuilt from the sitemap by `scripts/refresh-docs-mirror.py`: 382 pages (was 55), 9 skills at the 2026-09-14 build, `MANIFEST.json`. Observed: `pages in sitemap: 382`, `pages_failed: []`, `skill_files_failed: []`. 42 of the 55 old pages had changed; the code-relevant changes are in `findings.md` rows A8, B6, B17, C11, C12, E1, F3.
- [x] 2026-09-14 Bruno collection (`bruno/`) — 47 files that cover every route in `backend/src/abdm/urls.py`: Care login and refresh, 2 probes, 5 facility routes, 6 enrol steps, 3 login steps, the transaction read-back, 3 patient routes, the 2 callback probes, and all 13 callback receiver paths. Bodies come from the docs mirror; the 3 bodies that the docs do not publish are marked as placeholders (see `docs/findings.md`, 2026-09-10). Observed: `@usebruno/lang` parsed 47/47 files with 0 failures; `bru run probes --env local` gave `health 200` with the assertion passed and `gateway-status 403` without a token; `bru run callbacks/link-on-carecontext.bru --env-var careUrl=http://localhost:8099` reached a local echo server on path `/api/abdm/v3/link/on_carecontext` with a fresh `REQUEST-ID` UUID and a UTC `TIMESTAMP` with milliseconds. Not yet run against the sandbox with real credentials (user-driven).
- [x] 2026-09-15 Bruno collection updated to the ADR-011 routes: 58 files parse (`@usebruno/lang`), 46 requests; the route diff against `backend/src/abdm/urls.py` is empty both ways. The 7 files that carried a pasted `Authorization: ******` were fixed; the 2 Care JWTs that sat in `environments/local.bru` were removed and the variables are now secret.
- [x] 2026-09-19 Docs mirror rebuilt after the site republished on 2026-09-16 (catalogue 2026.09.16). Observed: `pages in sitemap: 450`, `pages_failed: []`, `skill_files_written: 47`, `skill files stale (removed): 11`. 441 of 450 pages changed or are new; 320 old files left (299 flat endpoint pages, `api/m1/apis`, `api/m4/undocumented`, `resources/testing/*`, 6 `whats-new` pages). `scripts/generate-error-catalogue.py` refused the new `reference/error-codes` page (exit 2, "no `What to do` column"), so `error_catalogue.py` is unchanged. The code-relevant changes are in `01-sources.md` §What changed and in `findings.md` rows A10–A12, B19, E15.
- [x] 2026-09-21 Docs mirror rebuilt (`python3 scripts/refresh-docs-mirror.py --diff`, then the real run). Observed: `pages in sitemap: 450`, `pages changed or new: 5` (cosmetic), `skill files changed or new: 26`, `skill files stale (removed): 0`, `skill_files_written: 50`, `pages_failed: []`. New: `references/design.md` in `abdm-m1`, `abdm-m2`, `abdm-m3`; a "survey the codebase first" loop in every `scaffold.md`. MCP `catalogue_info`: `built_at 2026-09-20T18:15:45Z`. What it changes for the code is in `01-sources.md` §What changed on 2026-09-21 and `findings.md` A13–A15.

## Phase 1 — Gateway session (prerequisite to everything)  [~]
Docs: `/docs/hiecm/v3/api/gateway`, `/concepts/gateway`, `/getting-started/first-fifteen-minutes`.
- Backend: `abdm/gateway/session.py` — `POST sessions` with clientId/secret, cache token until expiry, refresh. Header set (`X-CM-ID`, `REQUEST-ID`, `TIMESTAMP`, `Authorization`) exactly as `get_operation gateway-sessions-create` says.
- Backend: fetch RSA public certificate (M1 encryption) and gateway JWKS (`gateway-get-gateway-certs`) for callback signature verification (`/concepts/callback-authenticity`).
- [x] 2026-09-09 verified by curl from the workdir: `POST https://dev.abdm.gov.in/api/hiecm/gateway/v3/sessions` → HTTP 200, `{accessToken, expiresIn: 1200, refreshExpiresIn: 1800, refreshToken, tokenType: "bearer"}` — shape identical to the docs example. Token lifetime is 20 min; refresh path via `m1-token-refresh` still to confirm.
- [x] 2026-09-09 from inside Care: `abdm.gateway.session.get_access_token()` → token, Redis TTL 1140s (=1200−60 margin), second call served from cache. Exposed as `GET /api/abdm/gateway/status` (auth required, token never returned).
- [x] 2026-09-14 Gateway token refresh: not built, by design. `m1-token-refresh` is the ABHA user X-token refresh (`GET /v3/profile/account/request/token`, `R-token` header), already done in Phase 2. The docs publish no refresh endpoint for the gateway session token. The plug creates a new session on expiry (Redis TTL 1140 s); the docs allow this. Recorded in `findings.md` (2026-09-14, M1 gap review).

## Phase 2 — M1 Create: ABHA identity  [~]
Docs: `/milestones/m1`, `/api/m1`, `/api/m1/apis`, skill `abdm-m1`.
Mandatory set (from `/concepts/hip-hiu` capability table, private integrator):
1. ABHA creation by Aadhaar OTP (request otp → verify → address suggestions → claim address → enrol byAadhaar).
2. Login by mobile / Aadhaar / ABHA number / ABHA address (request → verify → select account).
3. Fetch profile, download ABHA card.
4. Communication mobile number (post-enrol).
5. Local Luhn (ABHA number) and Verhoeff (Aadhaar) validators.
Also mandatory (MCP `x-abdm-requirement.level: mandatory`, case `SHARE_PATIENT_PROFILE_701`):
6. Scan and Share: receive `patient-share/v3/share`, answer `m1-on-share-acknowledgement` with a token number.
7. Consent record before enrolment (`CRT_ABHA_102`).
Optional for a private integrator (`milestones/m1`, rewritten after NHA's review of 11 September 2026): face authentication, fingerprint/iris, mobile/email update, find a forgotten ABHA. **None of these is built.** An earlier roadmap line said they were built on 2026-09-14; that was wrong and is corrected here (2026-09-15). Not for private integrators: demographic authentication, child ABHA. Not recommended by NHA: enrol by document. Decision (Rithvik, 2026-09-15): M2 first; optional M1 features later.

- Backend: `/api/abdm/abha/...` thin endpoints wrapping the ABHA service, doing RSA encryption server-side, holding the `X-token` server-side per transaction. Persist `AbhaNumber`-ish record linked to `Patient` (ADR-004).
- Frontend: `PatientRegistrationForm` slot — create/verify ABHA, then set fields on the host form; `PatientDetailsTabDemographyGeneralInfo` — show ABHA, card, status; `PatientSearchActions` — find by ABHA.
- Done when: sandbox test-data ABHA created/logged in from the CARE UI end to end; matrix in `.agent/skills/abdm-m1/references/test.md` mapped to observed results.
- [x] 2026-09-09 Journey 1+2 (Aadhaar enrol, mobile OTP, address claim) — backend + `AbhaWizard`; all six paths reach ABDM validation (real Aadhaar run pending, user-driven).
- [x] 2026-09-09 Journey 5 (login by mobile OTP → accounts → select) — backend `service.login_*`, wizard "Link existing ABHA" mode. Not yet run against sandbox with a real mobile.
- [x] 2026-09-09 Journey 7 (card) — proxied via stored X-token, `GET /api/abdm/patients/<id>/abha/card`; 409 when no valid token. Content-type undocumented; passthrough. The QR code (`/v3/profile/account/qrCode`) was built, then removed on 2026-09-10: the QR is a patient-held credential, so a staff panel is the wrong place for it. The staff-side use is a scan in `PatientSearchActions`, not a preview.
- [x] 2026-09-09 Linkage to Patient: `extensions.abdm.txn_id` → `post_save` → auto-maintained identifiers + `instance_identifiers` rebuild (ADR-004 amendment). Verified via in-process `POST /api/v1/patient/` and `PUT` (identifiers survive edit). Existing-patient link endpoint verified likewise.
- [x] 2026-09-09 UI: `AbhaPanel` on Demography tab (link / card / copy), status chip in `PatientHomeActions`. Not visually verified by the agent.
- [x] 2026-09-10 `PatientSearchActions` — "Find by ABHA" on the patient search page (`patient-search-actions.tsx`). Runs the link wizard (default tab ABHA number), then the backend reports `existingPatient` (lookup via `AbhaNumberIdentifier.find_patient` / address) → "Open patient" or "Register with ABHA" (`/patient/create?abdm_txn=<txnId>` → `patient-registration-form.tsx` reads `GET abha/transactions/<txnId>` and prefills name/DOB/gender/mobile/address/pincode). Core `?identifier=` search deliberately not used: ABHA is verified by ABDM before Care trusts it. Observed: tsc/eslint/build clean, `remoteEntry.js` 200 with CORS `*` on :4174; UI not visually verified by the agent.
- [x] 2026-09-10 Login by ABHA number / ABHA address / Aadhaar — one endpoint `POST abha/login/request-otp {hint, login_id, otp_system}` + `verify` (`abdm/abha/service.py::login_*`, `client.login_request_otp/login_verify_otp`; address goes via `/v3/phr/web/login/abha/*`). Non-mobile verify returns an X-token directly → `_finalize_login` also does `GET /v3/profile/account` for the full profile. Observed: in-process `APIClient` smoke (`/tmp/abdm_login_smoke.py`, ABDM client mocked) — validation 400s, txn kinds, `existingPatient` lookup. **Not yet run against sandbox with real OTPs (user-driven).**
- [x] 2026-09-10 X-token refresh — `AbhaTransaction.refresh_token_expires_at` (migration 0002), `service.ensure_x_token()` refreshes via `GET /v3/profile/account/request/token` (`R-token` header) when the X-token is expired and the R-token is not; the card uses it. Observed in-process: expired X + valid R → refreshed + rotated; both expired → `NoUserSession` (409). The `R-token` prefix was a docs conflict (findings C8). The live page (2026-09-15) sends it bare; the plug now sends bare first and retries with the prefix on 401.
- [x] 2026-09-11 Aadhaar login always uses the Aadhaar-linked mobile — the "Send the OTP to" selector stays only for the ABHA number and the ABHA address (`abha-wizard.tsx::canChooseOtpSystem`); `LoginOtpRequest` sets `otp_system="aadhaar"` for the `aadhaar` hint, because UIDAI sends that OTP. Observed: `manage.py shell` — `aadhaar`+`abdm` → `aadhaar`, `aadhaar` with no value → `aadhaar`, `abha-number` keeps both values; `npm run build` and `npx eslint src` clean (0 errors); 8 backend tests OK.

- [x] 2026-09-14 Consent record (`CRT_ABHA_102`) — the wizard has a consent checkbox (`abha-wizard.tsx`, id `abdm-consent`); "Send OTP" stays disabled until it is ticked. `POST abha/enrol/aadhaar/request-otp` needs `consent: true` (400 otherwise). `AbhaTransaction` stores `consent_recorded_at`, `consent_code`, `consent_version` (migration 0004); `GET abha/transactions/<txnId>` returns `consentRecordedAt`. Observed: `manage.py check` clean; `makemigrations --check` clean; 14 backend tests OK; `npm run build` + `eslint` 0 errors.
- [x] 2026-09-14 Scan and Share (`SHARE_PATIENT_PROFILE_701`) — callback route `patient-share/v3/share` → `AbdmCallback` (operation `m1-receive-patient-share`) → `tasks.dispatch_callback` → `share/service.py::handle_profile_share`: facility by `metaData.hipId` (fallback `facility_id`), counter check, patient match by ABHA identifiers, token `<context>-<nnn>` per counter per day, `AbhaTransaction` kind `profile_share` for the registration prefill, then `outbound.send("m1-on-share-acknowledgement", "/api/hiecm/patient-share/v3/on-share")` with SUCCESS or FAILURE. Model `AbdmProfileShare` (migration 0005 then; migrations restarted at 0001 on 2026-09-15). Facility config gains `counters`. Desk API `GET/POST facilities/<id>/abdm/profile-shares[...]`. UI: setup page card "Scan and Share counters" with one QR code per counter (`qrcode.react`, URL from `ABDM_SHARE_QR_URL_TEMPLATE`); patient search "Shared profiles" inbox (`profile-share-inbox.tsx`, polls 10 s) → open patient / register prefilled. Bruno `scan-share/`. Observed in-process (`share_smoke.py`, signature + gateway mocked): 5 shares → 202 each; tokens `OPD1-001`, `OPD1-002`, `Pharmacy-001`; unknown counter → FAILURE `HIP_UNKNOWN_COUNTER`; wrong intent → FAILURE `HIP_UNSUPPORTED_INTENT`; inbox hides `kycPhoto`, detail `?photo=1` returns it; prefill txn returns name/pincode; dismiss removes the row; callback `processed_status=handled`; bad counter `OPD 1` → 400. Frontend build + eslint clean.
  - [ ] sandbox: QR URL format. The docs do not publish it (findings). USER scans a known sandbox facility QR with a plain QR reader, reports the URL, and sets `ABDM_SHARE_QR_URL_TEMPLATE` (placeholders `{hip_id}`, `{context}`).
  - [ ] sandbox: real share from the PHR app → callback signature accepted, token shown in the app within 30 s, row in the inbox. Needs Celery worker running and the tunnel up.
  - [ ] sandbox: `on-share` host. The M1 page says `https://dev.abdm.gov.in/api/hiecm`; the callback page curl shows `abhasbx`. The plug uses the gateway host. Confirm by the `AbdmOutboundRequest` row.

- [x] 2026-09-17 A login with no ABHA offers the create journey — the desk sees `ABDM-1114` ("No ABHA user registered with this Aadhaar number.") or an empty account list, and the error now carries one action, "Create a new ABHA" (`abha-wizard.tsx::createFromLink`, `AlertAction` slot, so the dialog layout does not shift). The action drops the failed login transaction, moves to the create journey and carries what the desk typed: the Aadhaar for the `aadhaar` identifier, the mobile for the `mobile` identifier. The back arrow returns to the link step. The action shows for all 4 identifiers. Observed: `npm run build` → `dist/assets/remoteEntry.js`, built in 1.03 s; `npx eslint src` 0 errors (10 pre-existing warnings); locale check 217 keys, 158 used, 0 missing.
  - [ ] USER: on the sandbox, run a link by Aadhaar for an Aadhaar with no ABHA, press "Create a new ABHA", and confirm the Aadhaar is already filled and the create journey completes. Screenshot.

- [x] 2026-09-19 A refused ABHA link answers HTTP 400, never HTTP 500. The desk used to read
      "Something went wrong": `LinkError` was a plain exception, and the `post_save(Patient)`
      receiver raises it inside Care's own patient viewset. `LinkError` is now a DRF
      `ValidationError` that carries `{"errors": "<sentence>"}`, which Care passes through
      (`02-care-host-contract.md`, "Errors from a plug signal") and care_fe shows in a toast. The
      3 sentences say what failed and what to do, and name no other patient record. The rule
      itself stands: 1 ABHA number is 1 person (`/concepts/phr`), and `find_patient` answers with
      1 row, so a second patient record would break discovery and consent for both. The
      registration form now reads `existingPatient` from the transaction, keeps the ABHA prefill,
      sends no `txn_id`, and shows an amber card with the other patient's name and an "Open
      patient" button — so "Register as new" saves the patient without the ABHA instead of
      failing at save. The link wizard shows a refusal on its own done step (`finishError`).
      **Observed: `abha_link_conflict_smoke.py` → `LINK CONFLICT SMOKE OK — 14 checks passed, 0
      failed`, `cleanup done, rows left: 0`; the 400 body is `{"errors": "This ABHA is already
      linked to a different patient record. Open that patient record, or use a different ABHA."}`
      and patient B was rolled back; 90 backend tests OK; ruff clean; `npm run build` →
      `dist/assets/remoteEntry.js`, built in 1.01 s; `npx eslint src` 0 errors (10 pre-existing
      warnings).**
  - [ ] USER: on the sandbox, link an ABHA that another patient already holds, and confirm the
        message in the dialog. Then press "Register as new" from "Find by ABHA" and confirm the
        patient saves without the ABHA. Screenshot.

- [x] 2026-09-19 The ABHA dialog closes when the journey ends. The wizard never closes itself,
      so each caller must do it; 4 paths did not. The patient panel now closes the dialog when
      the link call succeeds (a refusal keeps it open and shows the reason). The registration
      form closes it as soon as the ABHA is applied, so the desk sees the card on the form.
      "Find by ABHA" and the Scan and Share inbox close before they open another page, so no
      dialog stays over the new page. The done step of "Find by ABHA" still waits, because it
      asks the desk to choose between "Open patient" and "Register as new".
      **Observed: `npm run build` → `dist/assets/remoteEntry.js`, built in 1.01 s; `tsc -b` exit
      0; `npx eslint src` 0 errors (10 pre-existing warnings).**
  - [ ] USER: on the sandbox, link an ABHA to a patient from the Demography tab and confirm the
        dialog closes and the panel shows the ABHA. Screenshot.

Pending sandbox observations (user-driven):
- [x] Real Aadhaar enrol run.
- [x] Login by ABHA number, ABHA address, and Aadhaar with real OTPs.
- [ ] X-token refresh, including the `R-token` prefix conflict (findings #6).
- [ ] Card response bytes and content-type (findings gap #4).
- [ ] 2026-09-21 `abdm-m1/references/design.md` (new) against the wizard, next pass (Rithvik: M1 changes only on a source change; this is one): branch on `refreshToken`, not on the login hint ("read the token, not the account count"); offer creation on `ABDM-1114` only, not on an empty accounts list; the resend button reuses a live transaction and shows a wait; inactive steps `inert`; the masked number the OTP went to on the OTP step; the account chooser for a mobile with several ABHAs. Source: `01-sources.md` §What changed on 2026-09-21.

## Phase 3 — M2 Attach: linking and sharing (the bulk)  [~]
Design: ADR-011 (lean M2), which keeps ADR-008 decisions 1–8. Facility setup: ADR-007 (now the sanctioned route).
Docs: `/milestones/m2`, `/api/m2`, `/concepts/linking`, `/concepts/data-flow`, `/concepts/encryption`, `/concepts/fhir`, skills `abdm-m2`, `abdm-fhir`. Fact sheet from the 2026-09-15 mirror read: `findings.md` sections E–I.

### Code-complete 2026-09-15 (ADR-011 rebuild)
- [x] Backend: `AbdmBridge`, `abdm/instance/`, the admin routes and `gateway/hrp.py` removed; `gateway/bridge.py` reads the bridge live and caches the id. New `abdm/hip/` (`rules`, `crypto`, `contexts`, `discovery`, `consent`, `transfer`, `views`) and `abdm/fhir/` (`bundle`, `op_consult`, `prescription`, `health_document`). 5 new tables. Migrations restart at `0001_initial`.
- [x] Frontend: instance dashboard `/admin/abdm` (gateway, bridge, services, HIP facilities, recent callbacks; all live) with an admin nav item; setup page has 2 cards (facility identity + HRP with a bridge status line, Scan and Share); new slots `EncounterActions` ("Link to ABHA" dialog with gateway activity) and `EncounterOverviewTop` (status row). `careApi.ts` mirrors `urls.py`.
- [x] Observed 2026-09-15: `manage.py check` — no issues; `makemigrations abdm --check --dry-run` — no changes; ruff — all checks passed; `python -m unittest` — 41 tests OK (checksums, crypto, callback signature, facility rules, share rules, hip rules, hip crypto, fhir bundles); `npm run build` — `dist/assets/remoteEntry.js` built; `npx eslint src` — 0 errors, 9 existing fast-refresh warnings; i18n — 205 keys, no missing key, no empty value.
- [x] Observed 2026-09-15: `m2_smoke.py` (session files; real Care auth and DB, ABDM network and signature mocked) printed `M2 SMOKE OK — outbound calls: 19 callbacks: 14` with 0 failed or unhandled callbacks. It proved: facility PUT → HIP ID derived; bridge GET live; admin overview lists the HIP facility (superuser only, 403 for staff); HRP body carries the cached bridge id and goes to the HSP host; Encounter save → `generate-token` with `X-HIP-ID`; `on-generate-token` → token active ~6 months → link call with `X-Link-Token`, reference = encounter `external_id`, display without clinical detail; `on_carecontext` → linked → notify → `on-notify` acknowledged; duplicate callback deduped; discovery → `on-discover` with the care context, no match → `patient: []`; link init → OTP session, masked hint → wrong OTP error block → right OTP `on-confirm`; consent GRANTED stored and acked, revoked via the `/api/v3` path; health-information request → ACKNOWLEDGED → 2 bundles (OPConsultRecord, PrescriptionRecord) pushed encrypted → **decrypted by the HIU-side key in the test, checksum = MD5 of the plaintext** → notify TRANSFERRED; a wider date range refused with `ABDM-1063` and acked ERRORED; SMS deep link body; non-superuser 403 on bridge registration; Scan and Share token `OPD1-001`.
- [x] Observed 2026-09-15: MCP `validate_fhir` returned `findings: null` (0 findings) for the OPConsultation, Prescription and HealthDocumentRecord bundles built from fixture CARE data (`fhir_smoke.py`, session files).

### Code-complete 2026-09-18 (ADR-013 staged sharing)
- [x] Backend: `AbdmShareItem` (migration 0002; sandbox history kept as linked items); `post_save` stages prescriptions (not draft), final diagnostic reports and discharge summary uploads, plus 1 outpatient OP consultation with the first record; the desk links selected items or excludes one; a completed/discharged Encounter links every staged item; retries hourly × 3 (configurable) via Celery beat; `hi_types` derived from linked items; transfer builds only from linked items. FHIR: `DischargeSummaryRecord` added, `HealthDocumentRecord` removed, OP consult outpatient only, Prescription from prescriptions only.
- [x] Frontend: encounter tab "ABDM Records" (`encounterTabs.abdm`); the actions slot opens it.
- [x] Observed 2026-09-18: `manage.py check` clean; `makemigrations --check` clean; ruff clean; 90 unit tests OK (`test_sharing_rules.py` added); `m2_smoke.py` → `M2 SMOKE OK — outbound calls: 31 callbacks: 54` with the new stages 3 (stage, draft, cancel → excluded), 3b (select 1 → token), 4 (link carries only the queued type), 5 (item linked, `hi_types` derived, unselected item stays staged), 5a (completion → all staged linked in 1 call with linked + queued types; excluded item not sent), 5c (refusal → hourly retry → failed after 1 + 3 → desk re-select resets → linked), 5d (exclude/include; a linked item refuses). `fhir_smoke.py` + MCP `validate_fhir`: OPConsultation, Prescription, **DischargeSummary** → `findings: none`. `npm run build` OK; eslint 0 errors; i18n 250 keys, none missing or empty.
- [ ] USER: write a prescription on an outpatient encounter of an ABHA patient → the ABDM tab shows `Prescription` and `OP consultation` as Staged and no gateway call; select one → Link selected → callback → Shared; complete the encounter → the rest links by itself.
- [ ] Sandbox measurement for findings F10: does a second link with a new `hiType` add the type at ABDM? Link Prescription first, then OPConsultation for the same reference; read the `hiType` list in a PHR app discovery.
- [ ] Discharge summary: generate one from an inpatient encounter (a `Template` row is needed), check it is staged, link it, confirm the bundle in the PHR app.

### Sandbox proofs (user-driven; each ends on an observed gateway answer)
- [x] 1. Facility and bridge.
  - [x] Observed 2026-09-14: `GET bridge-services` → HTTP 200, bridge `SBXID_035123`, url `https://care-abdm-sbx.rithviknishad.dev/api/abdm`, `services: []`.
  - [x] Observed 2026-09-14: HRP register → HTTP 200 error 2500 "Provided facility name is not matched with registered name" (user screenshot). Host fixed to `ABDM_HSP_URL`.
  - [x] Observed 2026-09-15 (user): bridge URL registered; HRP register → HTTP 200, `hrp_registered_at` 16:02 IST. `GET /api/abdm/admin/overview` shows the service `{"id": "IN1410000232_1", "name": "FACILITY WITH P", "types": ["HIP", "HIU"], "active": true}` (findings B18: the real row shape differs from the docs).
  - [x] Observed 2026-09-15 (Playwright run, session files `dogfood-output/report.md`, 16 screenshots): `/admin/abdm` shows the gateway badge, bridge card (registered), 1 service `IN1410000232_1 HIP, HIU`, 1 HIP facility, "No callback received yet."; `/facility/<id>/abdm/setup` shows the 2 cards, `HIP ID IN1410000232`, `HRP service registered at 4:02:21 PM`, `Bridge registered`, the QR-template warning, save bar disabled; the encounter overview row reads `IPD records for 15 Sep 2026 · Waiting for the link token`; the dialog shows the ABDM-1092 waiting text; the patient panel shows `KYC verified`, number and address; patient search shows `Find by ABHA` and `Shared profiles`. Found and fixed the same day: "Open setup" from the admin page crashed the host (J7, now a full-page link); the dialog's Gateway activity missed the token requests (query widened); ~25 `modulepreload` 404s per page (J8, `modulePreload: false`). Not ours: `/facility/<id>` redirects home and `/admin` is Page Not Found in the host.
- [x] 2. Callback receiver. Observed 2026-09-17 07:02:47Z, the first real gateway callback: `POST /api/abdm/api/v3/hip/token/on-generate-token` (the docs omit the `/api`), 1.9 s after the 202, `Authorization: Bearer <RS256 JWT>` signed by the gateway's own RS256 key (`azp: gateway`), `X-HIP-ID`, `REQUEST-ID`, `TIMESTAMP`, `User-Agent: ReactorNetty/1.1.19`, source IP `13.203.243.253`. Row stored, signature `ok`, header name recorded (findings E2, E3, E12). The receiver had been proven reachable through Cloudflare with 7 User-Agents before that (no edge blocking).
- [x] 3. Link token. **Root cause of "no callbacks" found 2026-09-17:** every request since 2026-09-15 sent `X-HIP-ID: IN1410000232` (the HFR id, as the docs say) and the gateway accepts that with 202 but never delivers the callback. The gateway routes on the service id it issued at HRP registration, `IN1410000232_1`; with that header the callback arrived in 1.9 s and the token's own claims say `hipId: IN1410000232_1` (findings E1, B18). The plug now stores the service id read from `gateway-list-bridge-services` (after HRP registration and on every setup-page load) and a facility without one is not configured for M2. Token: RS512 JWT, `exp` = `iat` + 182.5 days (F2). The old 2026-09-15 sub-bullets are superseded by this one.
  - [x] 2026-09-17 07:10Z: the stored callback replayed through the fixed code → `AbdmLinkToken.active`, 4 `m2-hip-link-care-context` calls sent with `X-HIP-ID: IN1410000232_1` and `X-Link-Token` → **all HTTP 400 with an empty body** (findings E11). 20 controlled variants (docs example body, integer/absent ABHA number, single HI type, `Bearer` prefix, wrong HIP id, every header spelling) → the same empty 400; a missing header → empty 401 `Basic realm`, so the header names are right and the gateway's own link-token check is what fails. The token was 8 minutes old; the endpoint page says "must be called immediately before the linking call" (F1). The plug now regenerates a token older than 5 minutes and links from the callback that delivers it.
  - [x] 2026-09-17 07:27Z: fresh token request → error callback in 8.8 s: `ABDM-1027: You are blocked. Please try again after 24 hours.` (findings E14), caused by the burst of refused link calls. The plug recorded it on the token row and the 4 care contexts show the sentence on the desk. `gateway-list-bridge-services` and the session call keep working.
  - [x] 2026-09-17 11:19Z (16:49 IST): encounter `4ddc3e10-2354-4d86-9666-9d11505de359` created for patient 61 through the backend API alone (`POST /api/v1/auth/login/` then `POST /api/v1/encounter/`, class `amb`). The plug linked it without help: care context `OPD records for 17 Sep 2026`, hiType `OPConsultation`, `m2-generate-link-token` 202 at 11:19:55Z. The callback arrived 968 s later, exactly as E10 predicts. The ADR-012 leeway accepted it (signature `ok`, 447 s past the token `exp`, `processed_status=handled`), so the fix is proven against a real late callback. The body was `ABDM-1027` again, because the block follows the ABHA address (E14) and patient 61 is blocked until 2026-09-18 07:27Z.
  - [x] **2026-09-17 11:46Z to 11:56Z: check order on the link call (E11).** 9 controlled calls on `nihal_99@sbx` with the 6-month link token of row 13. (1) `patient: []` answers JSON `400 ABDM-9999: patient attribute required in the payload`, so ABDM reads our payload. (2) An invalid `hiType` answers the same empty 400 and not `ABDM-1006`, so a well formed request never reaches field validation. (3) The sibling endpoint `link/context/notify` reaches its backend with the same session token and the same `X-HIP-ID` and answers `ABDM-9999: User not found`, so our credentials and our HIP work next door. A `303001 ... SUSPENDED` answer was seen once here, but it carries little weight: rows 433 and 434 show the same state on the healthy `HIECM-LinkToken--vv3` endpoint at 13:29 IST, which worked again 3 minutes later. Every refusal comes from `server: istio-envoy` in 16-25 ms and carries `correlation-id` and `activityid`. The plug now stores the response headers of each refusal (`AbdmOutboundRequest.response_headers`, migration `0003`). Nothing on our side is left to vary. **Superseded 2026-09-18: the "ABDM fault" reading was wrong. The check order proves only that the token and the top-level shape pass; it does not cover the field that was the cause.**
  - [x] **2026-09-18: escalation is not needed. The cause was ours (E11): `hiType` must be a JSON string, not an array.** Measured with 1 variable changed at a time, same token and headers: the array answers the empty 400; the string answers **202**. Raise a docs defect instead, because the page types `hiType` as `object[]` and its example sends an array.
  - [x] **2026-09-18 09:52Z: the real link ran for encounter `4ddc3e10-2354-4d86-9666-9d11505de359`.** Link token → `m2-hip-link-care-context` **HTTP 202** → `on_carecontext` `Successfully Linked care context` → `AbdmCareContext.status=linked`. Care context 17 (2 HI types, 1 `patient` block for each, F7) also answered 202 and reached `linked`.
- [x] 4. HIP link: `on_carecontext` lands → `AbdmCareContext.status=linked`. **Observed 2026-09-18 on care contexts 20 and 17: the success `status` is `Successfully Linked care context` and the callback carries no `error` block (F4 answered).**
- [x] 5. Notify: `on-notify` acknowledgement **SUCCESS**, observed 2026-09-18 on care contexts 20 and 17. 2 defects were found and fixed on the way: `careContext.patientReference` must hold our internal patient reference, not the ABHA address (F8), and the notify must wait for ABDM to index the link (F9).
- [ ] 6. Discovery from a PHR app → `on-discover` 202 → the PHR app shows the visit. Record the real `verifiedIdentifiers` shape (findings G1).
- [ ] 7. User link: PHR app selects the visit → OTP SMS → confirm → linked. Confirm the docs-inbound init body and the confirm `token` type.
- [ ] 8. SMS deep link: sandbox SMS received; `on-notify` SUCCESS.
- [ ] 9. Consent granted in the PHR app → `AbdmConsent GRANTED`; record the real path and body (findings E3, H1).
- [ ] 10. Data transfer: PHR app requests records → push → notify TRANSFERRED → **record readable in the PHR app**. This is the docs' end-to-end loop (`/concepts/hip-hiu`).

### Error handling (ADR-012)  [x]
Every failure reaches the desk. Before this, `ABDM-1092` was shown as "Waiting for ABDM to confirm…"
and the spinner never stopped.
- [x] Generated catalogue: `python3 scripts/generate-error-catalogue.py` → 818 codes from 928 rows.
- [x] 1 pure classifier, `abdm/errors.py`. **Observed 2026-09-17: 78 backend tests, result OK**
      (`test_errors.py` added: 18 tests over the window, the block, the plug codes and the API shape).
- [x] The plug no longer causes `ABDM-1092`: the 5-minute freshness rule is gone and no link-token
      request goes out inside `TOKEN_REQUEST_WINDOW` (7 min; the measured boundary is 362–374 s, F6).
- [x] A late callback is no longer thrown away (`ABDM_CALLBACK_SIGNATURE_LEEWAY_SECONDS`, E10).
- [x] **Observed 2026-09-17 against the live database through the real state endpoint** (superuser,
      `APIClient`, no mocks). Every stuck row now carries a failure block:
      `ABDM-1092` → `wait`, `retryAt 10:07`, "ABDM refused a second request for this patient so soon
      after the last one."; a blank `pending` row → `NO_ANSWER`, `retry now`, "ABDM did not answer.";
      `HTTP_400` → `ask_support`, `retry never`, with the REQUEST-ID for support.
- [x] Desk: the `waiting` view holds the button until the time shown. Admin: the last failure per
      facility. **Observed: `npm run build` OK, `dist/assets/remoteEntry.js` written; `tsc --noEmit`
      clean; `eslint src` 0 errors.**
- [x] Closed 2026-09-18: `m2-hip-link-care-context` answers **202** and the context reaches `linked`. The empty 400 was an array `hiType` (E11). ADR-012 still gives support the reference when a call does fail.

### Known limitations after this pass
- A record is announced to ABDM only when the desk links it or the Encounter closes (ADR-013). ABDM's own "a linked context gained records" notify follows each link.
- Record types: OPConsultation (outpatient), Prescription, DischargeSummary (PDF). DiagnosticReport is staged and linked, but its structured `DiagnosticReportRecord` bundle is not built yet (a data request for it answers ERRORED for that entry). WellnessRecord, ImmunizationRecord, Invoice are deferred (findings I4).
- The consent artefact signature is stored, not verified (findings H2).
- The link-confirm OTP is fixed (`123456`) outside production, like Care core's login OTP.

## Phase 4 — M3 Retrieve (HIU)  [~]
Design: ADR-014. Docs: `/milestones/m3`, the 12 flow pages under `/api/m3/endpoints/m3-consent-management-data-flow-hiu/`, `/concepts/consent`, `/concepts/data-flow`, skill `abdm-m3` (2026-09-16 build: "Nothing here has been run against the ABDM sandbox").

### Code-complete 2026-09-19 (ADR-014)
- [x] Backend: `abdm/hiu/{rules,service,views}.py`; 4 tables (`AbdmConsentRequest`, `AbdmConsentArtefact`, `AbdmFetchRequest`, `AbdmFetchedRecord`), migration `0003_hiu_consent_requests`; `outbound.send(role="hiu")` sends `X-HIU-ID`; 6 callback paths (`/v3/hiu/consent/request/on-init`, `on-status`, `notify`, `/v3/hiu/consent/on-fetch`, `/v3/hiu/health-information/on-request`, and our data push URL `/v3/hiu/health-information/transfer`); periodic `hiu_housekeeping` (15 min); plug codes `NO_DATA`, `CHECKSUM`, `DECRYPT_FAILED`, `CONSENT_NOT_LIVE`, `NO_CALLBACK_URL`. Desk routes: `GET/POST patients/<id>/abha/consent-requests?facility=`, `POST .../<id>/refresh|fetch`, `GET patients/<id>/abha/records/<id>`, `GET providers?name=`.
- [x] Frontend: 3rd card "Records from other facilities" on the ABDM Records tab (`fetch-records-card.tsx`), request dialog (purpose, 8 record types, date range, validity 7/30/90/180 days, optional provider picker), FHIR document viewer (Composition sections, per-resource summaries, attachments, raw JSON, download). 93 new locale keys.
- [x] Observed 2026-09-19: `manage.py check` — no issues; `makemigrations abdm --check --dry-run` — no changes; `migrate abdm` — `0003_hiu_consent_requests... OK`; ruff check and format — clean; `python -m unittest` — **118 tests OK** (`test_hiu_rules.py`: 28 tests, every parser run on the docs example body of its page; M3 paths in `test_callback_paths.py`); `npm run build` — `dist/assets/remoteEntry.js` built in 0.95 s; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — 345 keys, 0 missing, 0 empty; Bruno — 60 requests parse, route diff against `urls.py` empty both ways.
- [x] Observed 2026-09-19: `m3_smoke.py` (session files; real Care auth and DB, ABDM network and signature mocked; the script plays the HIP) printed **`M3 SMOKE OK — outbound calls: 16 callbacks: 11`**, 75 checks. It proved: init body and `X-HIU-ID` (no `X-HIP-ID`), requester = medical council registration; `on-init` → id stored; refresh → status call → `on-status`; GRANTED notify → ack names the artefact and the notify REQUEST-ID → `consent/fetch`; `on-fetch` → detail stored → HI request with our key material and `dataPushUrl`; `on-request` → transaction id; **a HIP-side push of 2 bundles encrypted against our published key → both decrypted, HI types read off the Composition profiles, 1 MD5 mismatch flagged and kept → notify RECEIVED as HIU → ciphertext scrubbed from the stored callback**; record detail; duplicate push deduped; fetch again → 2nd request, refused while in flight; housekeeping fails a stale request and notifies FAILED; REVOKED → artefact and request revoked, records erased, detail 410; DENIED with reason, ack carries the request id; refused init → 502 with the failure block; erase-at housekeeping; validation 400s; providers proxy (200, 204 → `[]`, short query 400); 403 for a user without `can_view_clinical_data`.

### Observed on the sandbox 2026-09-19 (Rithvik's admin dashboard screenshot)
- [x] `m3-consent-request-init` refused: **ABDM-9999 "Invalid from/to date and Date must be a present/before date"**. Cause: the dialog sent `dateRange.to` as the end of the current day, a future instant. Fix (findings L9): `hiu/rules.validate_request` clamps `date_to` to now; `hiu-state.fromDateInput` never passes now. Observed: 153 unit tests OK (`test_validate_clamps_the_range_end_to_now`); `M3 SMOKE OK — outbound calls: 18 callbacks: 11`.
- [ ] Re-run proof 1 below after the fix: the init is accepted (202) and `on-init` arrives.

### Sandbox proofs (user-driven; each ends on an observed gateway answer)
- [ ] 1. `consent/v3/request/init` → 202 → `on-init` with a `consentRequest.id`. Confirms `X-HIU-ID = IN1410000232_1` and the `hip`-less body. Record the real body (findings L1).
- [ ] 2. The request shows in the patient's PHR app with the purpose, types, dates and validity we sent.
- [ ] 3. Grant in the PHR app → `notify` GRANTED → ack 202 → `consent/fetch` 202 → `on-fetch`. Record the artefact detail shape and where `dataEraseAt` sits.
- [ ] 4. `health-information/request` 202 → `on-request` with a transaction id → the other HIP's push lands on `/api/abdm/v3/hiu/health-information/transfer`. Record the push `Authorization` token (`azp`) and whether the gateway JWKS verifies it (L6).
- [ ] 5. Records decrypt; MD5 matches; `notify` RECEIVED 202; the viewer shows the bundle.
- [ ] 6. Deny in the PHR app → DENIED notify; the ack with the request id is accepted (L4).
- [ ] 7. Revoke in the PHR app → REVOKED notify → records erased.
- [ ] USER: on the sandbox, with a 2nd facility (or this facility as its own HIP) that holds a linked care context for the patient, run 1 to 5 from the ABDM Records tab and report screenshots.

## Phase 5 — M4 Enrol: the NHPR from CARE  [~]
Design: ADR-015 (2026-09-19; supersedes the 2026-09-15 note that "no M4 API build is required"). Docs: `/milestones/m4` (4 journeys, 100 operations since 2026-09-16), `/registries/nhpr`, `/registries/nhpr/hpr`, `/registries/nhpr/hfr`, the flow pages under `/api/m4/endpoints/`, skill `abdm-m4`.
- [x] 2026-09-15: the docs sanction the NHPR portal route; ADR-007 facility setup and the HRP linkage exist. Rithvik, 2026-09-19: build the API route too, "everything, A to D", tested together with M3.

### Code-complete 2026-09-19 (ADR-015)
- [x] Backend `abdm/nhpr/`: `rules.py`, `crypto.py` (RSA/ECB/PKCS1 with the NHPR certificate), `client.py` (44 wrappers + 27 master kinds, cached), `facility.py` (lookup, search, link, programme OTP, the 5-step HFR onboarding), `professional.py` (login by password or Aadhaar OTP, HPID creation, register, documents, info), `views.py`; 4 tables (`AbdmHprProfile`, `AbdmHprLogin`, `AbdmHpidTransaction`, `AbdmHfrOnboarding`; the 4th removed the same day by ADR-016), migration `0004`; 16 routes; `outbound.send(role="none")`; the HRP linkage body names `HIP` and `HIU`; the M3 consent `requester.identifier` carries the HPR ID when the user linked one; plug codes `NO_HPR_SESSION`, `NO_HPR_ID`, `OTHER_HPR_ID`, `NOT_FOUND`, `NO_TOKEN`, `NO_TXN`, `NO_TRACKING_ID`, `HFR_ERROR`, `NHPR_ERROR`.
- [x] Frontend: HFR card on the setup page (lookup by ID with the registry record, search by name and LGD state, link, programme OTP, wizard entry); wizard page `/facility/:facilityId/abdm/hfr/register` (HPR login gate, dedup, basic with 2 photos and opening hours, services and programmes, details with specialities per system of medicine and bed counts, submit); "My HPR ID" page under the user menu (`/facility/:facilityId/users/:username/abdm-hpr`): login dialog with the public record check, HPID creation wizard (Aadhaar link, 5-second poll, mobile match or OTP, username suggestions, create), the full register-professional form (5 blocks, dynamic registrations and qualifications, certificates), documents, professional info; `master-select.tsx` over 27 code lists. 305 new locale keys.
- [x] Observed 2026-09-19: `manage.py check` — no issues; `makemigrations abdm --check` — no changes; `migrate abdm` — `0004_nhpr_hpr_hfr... OK`; ruff — clean; **`python -m unittest` — 149 tests OK** (`test_nhpr_rules.py`: 24 tests, every parser on its page example; `test_nhpr_crypto.py`: 3 tests, PKCS1 round trip and 6 certificate shapes); `npm run build` — `dist/assets/remoteEntry.js` built in 1.1 s; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — 657 keys, 0 missing, 0 empty; Bruno — 77 requests parse (`nhpr/` folder, 17 requests), the route diff against `urls.py` is empty both ways.
- [x] Observed 2026-09-19: `m4_smoke.py` (session files; real Care auth and DB; the script is the NHPR and holds the RSA private key) printed **`M4 SMOKE OK — NHPR calls: 50 total calls: 54`**, 71 checks. It proved: A, lookup by ID (case-insensitive, 404 and 400 paths), search by name and state, link → registered name and default HIP name stored, HIP ID re-read; HRP body with `HIP` and `HIU` and the registry name; facility OTP send and validate. B, `verify-id`, password login (body as the page; token held; `account/information` with `x-hprid-auth` and the HPR bearer; no photo or token in the state), logout blanks the token, Aadhaar-OTP login (init → wrong OTP 400 → verify), a 2nd HPR ID refused, mobile OTP refused with the gap named; **the M3 consent init now carries `{"value": "71266557771234", "type": "HPR_ID", "system": "https://hpr.abdm.gov.in"}`**. C, dedup (no HPR header) → basic (`x-hprid-auth`, empty `trackingId`, photo bytes not stored) → additional → detailed (a stray field refused before any call) → submit (`x-hprid-auth`) → `IN` id found by the name search → the Care facility linked; a write without an HPR session → 400 `NO_HPR_SESSION`. D, `generateLink` → poll false → poll true (details, no photo exposed, photo held) → mobile match false → OTP (**mobile and OTP decrypted by the NHPR private key, PKCS1**) → suggestions → create (**password decrypted**, Aadhaar photo and split name in the body) → profile created, token held, photo blanked → register the full profile (`hprToken` + form; a stray block refused) → document slots → upload → professional info; an existing account on poll is linked, not created. E, masters normalised (LGD children, HPR sub-categories, courses POST) and cached.

### Code-complete 2026-09-19 (ADR-016: facility onboarding UX)
Rithvik's 4 follow-ups: field help on every M4 field; 1 registry card instead of 2; create a Care facility from a registry record; a step-by-step "Add a facility" with the Care-only or Care + NHPR choice. Decisions: nobody types a registry value; no new table (`AbdmHfrOnboarding` removed, state in `extensions["abdm"]`); the host `AddFacilitySheet` is replaced through the host's component-override registry, no host change.
- [x] Backend: `facility/service.py` (`EDITABLE_FIELDS = hip_name, counters`; `set_registry_link`, `get_onboarding`/`save_onboarding`, `is_linked`); `nhpr/facility.py` on extension state; `nhpr/rules.py` `care_prefill`, `suggest_care_facility_type`; `facility/create.py` (Care's own `FacilityViewSet.handle_create` in-process, then the link); 4 routes under `organizations/<id>/` and `care/facility-form-options`; the extension schema marks `facility_id` and `facility_name` read-only and adds `hfr`, `hfr_onboarding`; the setup `PUT` body is `{hip_name, counters}`; the HRP registration refuses an unlinked facility. Migration `0004` edited (3 tables).
- [x] Frontend: `registry-card.tsx` (1 card, not-linked and linked states; `RegistryFinder`, `RegistryRecord` shared); the ADR-007 identity card and `hfr-card.tsx` removed; `add-facility-wizard.tsx` at `/abdm/facilities/new` (3 modes, URL-resumable, `?organization=` as optional geo context, the HFR wizard embedded, services step, done checklist); `care-facility-form.tsx` + `care-facility-draft.ts`; `geo-organization-picker.tsx` (cascading, leaf required, state and district prefilled by name); `add-facility-sheet-override.tsx` declared in the manifest `overrides`; the admin dashboard's **Facilities** card lists every facility with counts, a filter, Registry / Services / Problem columns (the problem as a code badge with a tooltip) and "Add a facility" (Rithvik, later the same day: no organization tab; `abdm-organization-tab.tsx` removed); `nhpr-field.tsx` + `useNhprHelp` with 125 fields of help (500 locale keys). Locale: 1231 keys.
- [x] Observed 2026-09-19: `manage.py check` — no issues; `makemigrations abdm --check` — no changes; `migrate abdm 0003` then `migrate abdm` — `0004_nhpr_hpr_hfr... OK` (3 tables; the orphan `abdm_abdmhfronboarding` dropped by hand); ruff — clean; **`python -m unittest` — 153 tests OK** (`CarePrefillTests`: prefill fields, bad values dropped, 9 type suggestions; the L9 date clamp); `m4_smoke.py` printed **`M4 SMOKE OK — NHPR calls: 58 total calls: 64`** — new checks: the registry snapshot on the extension with `linked_by`; a typed `facility_id` in the setup `PUT` ignored; the onboarding state in `extensions.abdm.hfr_onboarding` and absent from config readers; section G, "Add a facility": Care's type and feature lists served; registry search and lookup before a facility exists; prefill (Care fields, type `Private Hospital`, state and district matched by name, no link yet, default HIP name; unknown id → 404); Care's own validation error in Care's shape (`loc: ["facility_type"]`); create → 201 through Care's path (geo organization, creator, root organization; browser `extensions` ignored), linked, HIP name set, HIP ID synced; prefill then names the linked facility; a 2nd link of the same id → 400 `ALREADY_LINKED`; Care-only create with no link, the `organization` context filling the geo organization; the admin overview lists every facility, linked or not, with type, registry id and HIP ID; a user without `can_create_facility` → 403. `m3_smoke.py` — **`M3 SMOKE OK — outbound calls: 18 callbacks: 11`** and `m2_smoke.py` — **`M2 SMOKE OK — outbound calls: 33 callbacks: 61`** (both link through the registry mock now; +2 calls each). `tsc --noEmit` — clean; `npm run build` — `dist/assets/remoteEntry.js` built in 1.2 s; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — every literal key present; Bruno — 81 requests parse (`add-facility/` folder, 4 requests), the new paths match `urls.py`.
- [ ] Host setting for the override: add `AddFacilitySheet` to `REACT_MFE_REGISTERED_COMPONENTS` in the care_fe env (comma-separated with any other names) and restart the host dev server. Without it the host sheet stays and the ABDM admin dashboard is the entry.
- [x] Fix 2026-09-21 (Rithvik): the "Details in CARE" step showed every validation error at once, as soon as the person picked a mode on "Where is it?". It looked like a form submit, but the plug holds no `<form>` and every `<button>` carries `type="button"`. 2 causes: `GeoOrganizationPicker` reported its empty value in a mount effect, and the wizard marked the form as changed on every `onChange`. The wizard now shows the errors only after the person presses the create button (state `attempted`), and the picker reports a value only when the leaf organization changes. Observed 2026-09-21: `tsc --noEmit` — clean; `npx eslint` on both files — 0 errors; `npm run build` — `dist/assets/remoteEntry.js` built in 1.1 s.

### Observed on the sandbox 2026-09-21 (the first M4 calls; Rithvik's runserver log, then 40 read-only probes from `manage.py shell`)
The first real M4 call, the registry card's name search, was refused: `m4-facility-search -> HTTP 422`. The row was not in the database: the view raised a DRF `ValidationError` after the call and DRF's `set_rollback()` dropped it (J9). 40 read-only probes then fixed every parser and parameter against real answers. Every fact is in `findings.md` N1, N2, N4, N7, N8 (resolved) and N16–N22, J9 (new).
- [x] **Cause of the 422:** a name search needs `stateLGDCode` and `ownershipCode` (`HIS-1070 "Required OwnershipCode Field is empty."`; the page marks nothing required, N16). Fix: `nhpr/facility.search()` refuses a name search without a state or an ownership before any call, with the missing value named; the finder (`registry-card.tsx`) asks for the name, the state and the ownership (`OWNER` master: Government, Private, Public-Private-Partnership) and enables Search only when all 3 are set; both search routes take `ownership`; Bruno updated.
- [x] **Evidence survives a refusal (J9):** every M4 view returns the failure (`{errors, detail, code, requestId}`; 400 for a plug rule, 404 for not found, 502 for a registry refusal) instead of raising it; the HRP registration view too. The `AbdmOutboundRequest` row of a refused registry call stays.
- [x] **The registry's words reach the screen** (`abdm-m3/design.md`): a refused M4 call answers "The registry refused the request: HIS-1070 Required OwnershipCode Field is empty." (`nhpr/client.refusal_words`: the `details[]` lines, else the envelope message), and `detail` carries the same words for the admin page. Before, the person read "ABDM refused the request. Check the patient details" for a registry that holds no patient.
- [x] **Masters fixed against real answers:** `get-master-types` names 17 types (`MEDICINE, OWNER, CENTRAL-GOVERNMENT, PROFIT-TYPE, NON-PROFIT-TYPE, TYPE-SERVICE, SALUTATION, FACILITY-REGION, SPECIALITY-TYPE, ADDRESS-PROOF, FAC-STATUS, IT-EQUIPMENT, GENERAL-INFO-OPTIONS, IMAGING, DIAGNOSTIC, DAYS-OF-OPERATION, SOURCE`; N8). The wizard asked for `OWNER-SUBTYPE` and `OPERATIONAL-STATUS` (both HTTP 422) and hard-coded OPD/IPD; it now uses `FAC-STATUS`, `TYPE-SERVICE` (adds `DAY`), `SPECIALITY-TYPE`, `FACILITY-REGION`, and the owner subtype is the fixed list the registry accepts (`C` for government; `P`, `NP` for private and PPP; N17), served as the local master `owner-subtype-codes`, which then drives `get-owner-subtype` (ministries, or PP01–PP04 / PN01). `hpid/get/categories` and `subCategories` need `role` (else `HIS-3028 "Role is not valid"`; N18): the client sends it, the HPID wizard passes the chosen role. `medical-councils/name` takes `medicineName` (the page was right; the plug sent `name`). `parse_code_values` strips the padding the `OWNER` master and the languages carry (`"G         "`; N22), reads `districtName`, `subDistrictName`, `enShortName`, `medicalSystem`, and takes `id` over a slug `code` (the register body sends master ids). 4 masters that parsed to 0 rows (HPR districts, sub-districts, countries, systems of medicine) parse now.
- [x] **HPR ID check:** `searchByHprId` answers HTTP 422 `HIS-3008 "Invalid HPID."` for an unknown id, not the page's 404 (N20) → `NOT_FOUND`. The login dialog checks a complete id only (14 digits or `name@hpr.abdm`), not every keystroke, and shows a real refusal as itself.
- [x] **Setup page heals an old link:** a facility linked before ADR-016 (facility 9: id and name, no `hfr` record) gets its registry record on the first `GET` of the setup page (1 lookup); the linked card then shows the type and address lines.
- [x] **My HPR ID:** the 5-block register form opens on request ("Register professional profile" / "Update") instead of under the profile card of every person who linked an existing HPR ID.
- [x] **Settled without code change:** the gateway session token is accepted by the HFR search, every master, `auth/cert` and `aadhaar/generateLink` (HTTP 200 each; N1 in part — the login and the HPID creation writes are still to be seen); `auth/cert` returns a PEM public key as text/plain (N4); `generateLink` answers `{status: "URL GENERATED", txnId, url}` with the field `url` (N2); the real search row carries `systemOfMedicineCode`, `subDistrictLGDCode`, `villageCityTownName/LGDCode`, `workingInPsu`, `facPsuName`, `govtCategory`, `govtMinistries` beside the page's fields (N7 in part; kept in the parser); `facility-types` answers numeric codes (`5` Hospital, `105` Other) while the search row says `facilityTypeCode: "H"`, 2 code spaces (N21); a lookup of an unknown id is 200 with an empty list, not 404.
- [x] **Observed 2026-09-21:** `python -m unittest` — **157 tests OK** (`test_sandbox_shapes_of_2026_09_21`, `test_owner_subtype_codes`, `test_parse_facility_search_keeps_the_sandbox_codes`, `RedactHeadersTests`); ruff check and format — clean; `manage.py check` — no issues; `makemigrations abdm --check` — no changes; `m4_smoke.py` (the fake registry now answers with the sandbox's own refusal shapes) — **`M4 SMOKE OK — NHPR calls: 58 total calls: 64`**, new checks: a name search without ownership or state is refused locally with no call; the wrong-password 502 carries "Invalid credentials" and **its outbound row survives the request**; `verify-id` of an unknown id (registry 422 HIS-3008) → 404 `NOT_FOUND`; `tsc --noEmit` — clean; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — 504 used keys, 0 missing, 125 help fields; `npm run build` — `dist/assets/remoteEntry.js` built in 1.07 s; Bruno — 81 requests parse.
- [x] **Docs delta (`abdm-m2/design.md`, "Redact by name and length, never by value"):** `GET /api/abdm/callbacks/<id>` returns header names and lengths; a credential header or any JWT-shaped value is `<redacted, N characters>`. The database keeps the value for a replay through fixed code (2026-09-17).
- [ ] USER: on the setup page of "FACILITY WITH PATIENTS", confirm the card shows the registry record (type, address) after 1 reload; search the registry by name with Manipur and Private and confirm rows; run "Link my HPR ID" with your HPR ID and password → "Logged in"; report the log line or the screen for the first refusal, if any. Then the HFR wizard basic step: the ownership subtype and operational status pickers fill.
- [ ] Still unknown until a write runs (user-driven): whether `authPassword`, the HFR write steps and `createHprIdWithPreVerified` accept the gateway session token (N1); the `IN` id after submit (N7); the `x-hprid-auth` requirement per step (N9); the Aadhaar page flow (N2 in part).

### Code-complete 2026-09-21 (ADR-017: "My HPR ID" on the user profile)
Rithvik: the "My HPR ID" menu item navigated to `/facility/undefined/users/<name>/abdm-hpr` outside a facility, because the host builds a plug `userNavItems` URL with no fallback (`care_fe/nav-user.tsx:123-131`). The HPR API reads the caller only, so no facility and no username belongs in the path. Decision: the host slot `UserProfileSections` replaces the route, and the long flows move into sheets.
- [x] Frontend: `user-profile-hpr-section.tsx` (the slot; returns `null` on another person's profile; a summary card with the HPR ID, the HPR ID number, the role, the registration badge, the session badge and expiry, an account disclosure; a dropdown menu for log in again, read the registry, log out, unlink; 3 sheets for create, for the professional profile, for the documents); `hpr-documents.tsx` (the documents table, split out); `ui/sheet.tsx` and `hooks/use-mobile.ts` from the Care UI registry, with the portal given the plug container; `embedded` on `hpid-create-wizard.tsx` and `hpr-register-form.tsx` (no card frame, no header, because the sheet header carries the title; both close their sheet when they finish); `RegistryFacts` replaces the raw JSON dump; `hpr-page.tsx`, the 2 `abdm-hpr` routes and the `userNavItems` entry removed. Locale: 1237 keys (4 new, 2 dead ones removed).
- [x] Observed 2026-09-21: `tsc --noEmit -p tsconfig.app.json` — clean; `npx eslint src` — 0 errors, 10 pre-existing warnings; `npm run build` — `dist/assets/remoteEntry.js` and `user-profile-hpr-section-*.js` (89.18 kB) built in 1.18 s; i18n — every literal `abdm_*` key present; `grep -rn "hpr-page|abdm-hpr"` over `frontend/src` — no code reference left.
- [x] Inverted hero 2026-09-21 (Rithvik: "when linked/logged in, can we have inverted design? … similar to how we already have it in care context details"): a linked HPR ID now uses the encounter hero's shape — the `from-primary-700 via-primary-900 to-primary-950` gradient, 2 blur orbs, white words, and the 5 facts as `bg-white/5` tiles (`HeroFact`). The account disclosure and `RegistryFacts` take an `inverted` tone. The footer buttons are white with green words (`heroSolid`) or a translucent white face (`heroGhost`), because the green variants disappear on the gradient. The state with no HPR ID keeps the plain card. Observed 2026-09-21: `tsc --noEmit` — clean; `npx eslint` on the file — 0 errors; `npm run build` — `user-profile-hpr-section-*.js` (90.39 kB) built in 1.17 s.
- [x] Login dialog 2026-09-21 (Rithvik: the mobile-OTP note is unexplained; "the radio round thing is broken"): the method choice was a bare `<input type="radio">`, which the plug's base CSS leaves unpainted. It is now `ui/radio-group.tsx` from the Care UI registry (Base UI `Radio`), as 2 bordered rows that turn green when chosen (`has-[[data-checked]]`). The mobile-OTP note (findings N5) shows only when the person's public record lists a mobile method, and it now names the reason for that person. Observed 2026-09-21: `tsc --noEmit` — clean; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — 0 missing keys; `npm run build` — `dist/assets/remoteEntry.js` built in 1.17 s; the built CSS carries `.has-\[\[data-checked\]\]\:border-primary` and `data-checked:bg-primary`.
- [x] Coordinates 2026-09-21 (Rithvik's screen: `HIS-4019 Invalid Latitude … 1-6 decimal places` + `HIS-4020` on the basic step, with the prefilled value): findings N23. Care holds 16 decimal places and the registry search answers 15, so the HFR basic step always refused what the plug filled. `rules.coordinate(value, kind, strict=)` is the 1 place that shapes a coordinate: 6 places at most, 1 at least, out of range dropped (or refused by name, ADR-012). Used by the HFR prefill (`facility.hfr_state`), by `care_prefill` (so Care stores no more than 6), and by `basic_information_body` before the call. Observed 2026-09-21: `python -m unittest` — **160 tests OK** (`test_coordinate_keeps_1_to_6_decimal_places`, `test_basic_information_shortens_the_coordinates`, `test_prefill_rounds_to_6_decimal_places`); ruff check and format — clean; `manage.py check` — no issues; `makemigrations abdm --check` — no changes; on the real database facility 9 `32.0765414999999976` → `32.076541`, facility 10 `-126.3783070000000066` → `-126.378307`, facility 23 `10.0094250000000002` → `10.009425`.

  On the frontend, `shortenCoordinate(value, kind)` in `nhpr-shared.ts` mirrors the same rule for a value the person types: the 2 HFR basic fields and the 2 Care facility fields shorten on blur, so the person sees what the registry will get. Observed 2026-09-21 (the built source of the helper, run under node): `"32.0765414999999976"` → `"32.076541"`, `"25.635802000000098"` → `"25.635802"`, `"10"` → `"10.0"`, `" 76.368085 "` → `"76.368085"`, and an empty, unreadable or out-of-range value is left for the validator to report. `tsc --noEmit` — clean; `npx eslint src` — 0 errors, 10 pre-existing warnings; `npm run build` — `dist/assets/remoteEntry.js` built in 1.16 s.

### Observed on the sandbox 2026-09-21, afternoon (Rithvik's HPR login and HFR wizard run; 2 screens)
Every registry answer of the run is in the database now (J9). Facts: `findings.md` N24–N29, J10.
- [x] **"Logged in to the HPR as ."** (screen 2): the host's i18next has `skipOnVariables: false`, so `t(key).replace("{{id}}", …)` rendered every placeholder blank (J10). All 25 sites now pass values to `t()`; the line reads "Logged in to the HPR as Rithvik Nishad · rithviknishad@hpr.abdm". `hprSession` carries `name`. The profile had no name because `account/information` answers HIS-500 (N25); the profile fills itself from the public record (`searchByHprId`: name, both ids, category 100 Facility Manager, role 2) at login and, once, on the next read. **Observed on the real row (user 29) at 06:28Z: `name 'Rithvik Nishad' role 2 category '100'`**, filled by the running Care after the reload.
- [x] **The refusal text widened the page** (screen 1): 2 alerts said the same failure, and the registry's unbroken token `beds(countIPDBedsWithoutOxygen,…)` could not wrap. 1 `FailureNotice` now: the registry's lines as bullets, the code as a badge, the REQUEST-ID as a monospace reference; `min-w-0` and `overflow-wrap: anywhere` on every Alert and on the wizard's grid, so the page keeps its width. `HfrError`/`HprError` carry `details: [line, …]`; the onboarding state stores `error_details`.
- [x] **The rules the registry applied are checked before the call** (N27): the detailed step shows "Sum of IPD, HDU and day-care beds: 30. Total beds: 5. The 2 must be equal" and holds Save until they are; every system of medicine of the basic step starts with specialities available and names the ones with none; the basic step needs both photo names for the first save. Coordinates: `rules.coordinate` (N23, the other session's fix of the same afternoon).
- [x] **The HPR profile card shows the registry category** ("Facility Manager", from `hpid/get/categories?role=2`), beside the role. `fetch-documents-list` sends the 14 digits first (N26, unverified).
- [x] **Observed 2026-09-21:** 160 unit tests OK (with the other session's coordinate tests); ruff clean; `manage.py check` clean; `m4_smoke.py` — **`M4 SMOKE OK — NHPR calls: 58 total calls: 64`** with a smoke-owned superuser (the real one holds a real profile now); `tsc --noEmit` clean; `npx eslint src` 0 errors, 10 pre-existing warnings; i18n — 513 used keys, 0 missing, and no placeholder key called without options; `npm run build` — `dist/assets/remoteEntry.js` built in 1.17 s.
- [x] **The bed total is derived, never typed** (Rithvik, 2026-09-21: "total count can be derived right? default all to 0"). `nhpr/rules.medical_infrastructure()` reads the 10 counts as whole numbers (0 when absent, a negative or a word refused, a stray key refused), and sets `totalNumberOfBeds` to the sum of the 6 the registry sums; a typed total is ignored. The wizard shows the sum as a figure over the 6 counted fields, the 4 uncounted ones (ICU, ventilators, dental chairs) in their own group, every count at 0, and a hint when the sum is 0. The equality refusal cannot happen now. Observed: `test_bed_total_is_derived_from_the_6_counts_the_registry_sums` (the page example's 4 → 33); `m4_smoke.py` — "detailed body: every count sent, the total derived (5), a typed total ignored", **`M4 SMOKE OK — NHPR calls: 58 total calls: 64`**; 161 tests OK; `tsc` clean; eslint 0 errors; 515 used keys, 0 missing; `npm run build` — built in 1.14 s.
- [x] **A link by HFR ID was refused** (Rithvik's screen 2026-09-21: `HIS-4047 "Please enter valid resultsPerPage, Min value is 10, Max value is 15"`, findings N29). `facility.lookup()` sent `resultsPerPage: 5`, and the body builder allowed 1 to 50, so the 1 call behind "Link facility" could never succeed. Fix in the 1 shared place: `rules.RESULTS_PER_PAGE_MIN = 10`, `RESULTS_PER_PAGE_MAX = 15`, and `facility_search_body()` clamps every value into that range; `lookup()` takes the default. No caller can send a refused page size now. Observed 2026-09-21: `python -m unittest` — **178 tests OK** (`per_page=500` → 15, `per_page=5` → 10); ruff check and format — clean; **a real read-only call to the sandbox**: `facility.lookup("IN1410000232")` → `IN1410000232 Manipur Test Facility`, evidence row `m4-facility-search` HTTP 200 (before the fix the same call answered `HIS-4047`).
- [x] **The speciality code space was wrong** (Rithvik's screen 2026-09-22: `HIS-1070 "Invalid SpecialityCode provided for SystemOfMedicineCode -UN"`, findings N30). `get-specialities` answers `"UN-S68"`; `detailed-information` takes `"S68"` (its own example). The wizard fed the master code straight into the body, so a Details save could never succeed for any facility. Fix in the 1 shared place: `nhpr/rules.specialities()` strips a leading `<som>-` from every code, refuses a stray field, and `detailed_information_body()` runs it — no caller can send a prefixed code, a bare code is untouched. Observed 2026-09-22: `python -m unittest` — **179 tests OK** (`test_speciality_codes_are_sent_without_the_system_of_medicine_prefix`: `["UN-S68", "S100", " UN-S168 "]` → `["S68", "S100", "S168"]`); ruff check and format — clean.
- [ ] USER: reload the wizard's Details step: 1 notice with 2 bullets and the reference, the page keeps its width; the total shows as a figure and follows the 6 counts; the HFR session line names you. Then: pick specialities for M and SR, Save → the registry answer (N28 wants the submit body).
- [ ] USER: open the setup page of a facility, type the HFR ID (example `IN1410000232`) and link it. Expect the registry record, not the `HIS-4047` refusal.
- [x] **The HRP repeat refusal is now the answer, not a failure** (Rithvik 2026-09-22: `HTTP 200: 2500 Bridge-Id=SBXID_035123 is already associated with Hfr-Id=IN3210000772 for Service-Id=IN3210000772`, REQUEST-ID `ee389efa-03e9-4b74-8d49-f92aff95c5f6`, findings B20). HRP registration is once per (bridge, facility), and the refusal names the service id — which is the HIP ID (E1). Two fixes in the shared helpers: `facility/rules.already_associated_service()` reads `Hfr-Id`/`Service-Id` and returns the id **only when it is this facility**, so a name-mismatch 2500 still fails; `gateway/bridge.register_hrp_service` returns `{already_registered, service_id}`; `facility/service.register_hrp_service` stores the id when the bridge listing lags. And the resync: `facility/service.sync_hip_id()` now fills `hrp_registered_at` and clears `last_error` whenever it finds a live service, so a facility whose HRP was registered by the M4 flow reads right without a registration call. Observed 2026-09-22: `python -m unittest discover` — **182 tests OK**; ruff check and format clean; `manage.py check` — no issues; `makemigrations abdm --check` — no changes; `tsc --noEmit` clean; in-process smoke (`/tmp/hrp_already_smoke.py`, real `bridge` + `facility.service` with the sandbox body) — **`HRP ALREADY-ASSOCIATED SMOKE OK — 15 checks, 0 failed`** (the refusal yields `IN3210000772`, a refusal about another facility and a name-mismatch 2500 still raise, the resync fills the date once and keeps it).
- [ ] USER: press "Register as HIP and HIU" on the SECONDARY FACILITY again. Expect no error: the card shows the HIP ID and a registration date, and `/admin/abdm` lists the service.
- [ ] N31 (blocked on NHA): the bed sum set differs per facility type — the SECONDARY FACILITY run of 2026-09-22 was refused with `"Total count of beds(countDayCareBedsWithoutOxygen,countDayCareBedsWithOxygen) should be equal to TotalNumberOfBeds"`, not N27's 6-field rule. No code change made; do not hard-code a second set.

### UI proofs (user-driven; a screenshot each, recorded here)
- [ ] a. Setup page: 1 "Health Facility Registry" card; no typed HFR ID or name input anywhere; the linked state shows the registry record read-only, the HIP name, the HIP ID, "Register as HIP and HIU".
- [ ] b. `/admin/abdm` → Facilities card: every facility, the 4 count badges, Registry / Services / Problem columns; a long refused-call message is a code badge whose tooltip holds the words, and the table keeps its width on a narrow window; "Add a facility" opens the wizard (full page load).
- [ ] c. Organization facilities page with `REACT_MFE_REGISTERED_COMPONENTS=AddFacilitySheet`: the host "Add facility" sheet is replaced by the plug button; without the setting, the host sheet remains.
- [ ] d. Wizard, "already in the registry": search → pick → the Care form filled and marked "From the registry record"; state and district preselected; local body and ward picked; create → the done checklist shows linked and, after the services step, the HIP ID.
- [ ] e. Wizard, "not in the registry yet": create → the embedded HFR steps → submit → services; a refresh on any step resumes.
- [ ] f. A question-mark icon on every M4 field; 1 tooltip opened shows title, what, how, example.
- [ ] g. Wizard, "Care only": pick the mode on "Where is it?" → the details form shows no red error; press "Create" with an empty form → every error appears and the button turns grey.
- [ ] h. Your own profile page (`/users/<your name>`, and the same page inside a facility): the "My HPR ID" section is the last section. No "My HPR ID" item is in the user menu. A linked HPR ID shows the inverted hero card (the green gradient, white words, 5 tiles), like the care-context hero in the encounter ABDM tab. The 3 buttons open full-height sheets; the profile page does not move when a sheet opens. Another person's profile shows no such section.

### Sandbox proofs (user-driven; each ends on an observed registry answer)
- [ ] 1. Setup page → registry card → look up `IN1410000232` → the registry record shows the registered name → "Link" → the card turns to the linked state with that name. Record the real search row (N7 shape).
- [ ] 2. "Register HRP service" → the body carries `HIP` and `HIU` → 200 → both types on `/admin/abdm`.
- [ ] 3. User menu → My HPR ID → "Link my HPR ID" → the public record appears while typing → password login → "Logged in" → the profile shows the HPR ID in both forms. Record the `authPassword` and `account/information` answers (N9).
- [ ] 4. Aadhaar-OTP login: OTP arrives → verify → token.
- [ ] 5. HFR wizard on a test facility: dedup → basic (2 photos) → additional → detailed → submit → tracking id, then the `IN` id (N7). Record every 200 body and the master type names that worked (N8).
- [ ] 6. HPID creation with a 2nd Aadhaar (if a test identity exists): `generateLink` answer (N2), the Aadhaar page, mobile match, OTP, create; which bearer the HPID calls accepted (N1).
- [ ] 7. Register a professional profile: the accepted codes for salutation, category and work status (N6); the certificates; `fetch-documents-list`.
- [ ] 8. Raise an M3 consent request as that user → `requester.identifier.type == "HPR_ID"` is accepted by the HIE-CM.

## Phase 6 — Developer mode, hardening, publish  [~]

### Code-complete 2026-09-21 (ADR-018: developer mode, the audit explorer)
Design: ADR-018. Source: `abdm-m2/references/design.md` §"What the integrator needs on screen" (the 5 exchange states, redaction by name and length, the readiness check, inbound as a surface). Rithvik's 5 decisions: env var `ABDM_DEVELOPER_MODE=true`; any authenticated user when on; M1 joins the audit table with tokens redacted at write; the JSON tree written in the plug; core first.
- [x] Backend `abdm/dev/`: `redact.py`, `exchange.py`, `table_specs.py` (pure; 17 tables), `tables.py`, `readiness.py` (8 checks with a Celery worker heartbeat), `views.py` (9 read-only routes under `dev/`, 403 with the setting named when off). Migration `0005_developer_mode`: `AbdmOutboundRequest.request_headers` (names → lengths), `AbdmCallback.processing_error` (traceback) and `processed_at`. `outbound.record()` joins any transport to the audit table with redaction at write: `abha/client._call` (every M1 call, operation id in the docs' slug form `m1-post-v3-profile-login-request-otp`), the gateway session call (`gateway-sessions-create`; `clientSecret`, `accessToken` redacted) and the JWKS read. `tasks.dispatch_callback` stores the traceback of a handler that raises; the periodic tasks set the worker heartbeat.
- [x] Frontend `components/abdm/dev/`: `developer-page.tsx` at `/abdm/developer` (4 URL-addressed tabs: Exchanges, Inbound, Tables, Readiness; the off state names the setting), `exchange-list.tsx` (1 row per exchange, state chip, 5 s poll, new rows behind a "N new" button), `exchange-sheet.tsx` (timeline → request → answer → callbacks with traceback → rows named), `json-tree.tsx` (search, copy path and value, raw, lock badges for `<redacted, N chars>`), `timeline.tsx`, `inbound-list.tsx` (each gateway-started callback with its ack, or "No acknowledgement was sent"), `tables-browser.tsx` + `RowSheet`, `readiness-card.tsx`, `dev-footer.tsx` (the collapsed "Developer" disclosure appended last on the encounter ABDM tab, the patient ABHA panel, the facility setup page and the HPR section), `dev-state.ts`. The admin dashboard gains a "Developer explorer" card. 100 `abdm_dev_*` locale keys. `careApi.ts` mirrors the 9 routes; Bruno `dev/` folder (9 requests).
- [x] **Observed 2026-09-21:** `python -m unittest` — **178 tests OK** (`test_dev_redact.py` 6, `test_dev_exchange.py` 6 — the 5 states with the docs' figures 340 ms / 91 ms / 60 s, every sent operation has a kind, every ack and callback-call is really sent —, `test_dev_tables.py` 4 — every model has 1 spec, every named column exists, every secret column is declared —, `test_abha_audit.py` 1); ruff check and format — clean; `manage.py check` — no issues; `makemigrations abdm --check` — no changes; `migrate abdm` — `0005_developer_mode... OK`. **Real calls:** a forced gateway session and a JWKS read left 2 audit rows: `gateway-sessions-create` body `{"clientId": "<redacted, 12 chars>", "grantType": "client_credentials", "clientSecret": "<redacted, 36 chars>"}`, answer `accessToken: "<redacted, 1441 chars>"`, `refreshToken: "<redacted, 641 chars>"`; `gateway-get-gateway-certs` request headers `{X-CM-ID: 3, TIMESTAMP: 24, REQUEST-ID: 36, Content-Type: 16, Authorization: 1448}`. **`dev_smoke.py` — `DEV SMOKE OK — checks: 44`**: flag off → `{enabled: false}` and 403 naming the setting on 4 routes; on → status with the 5 states and the 600 s window; an M1 login through the mocked ABHA service → rows `m1-post-v3-profile-login-request-otp` (`loginId: "<encrypted, N chars>"`, header names + lengths) and `…-verify` (tokens `<redacted, 721 chars>`, the length kept); an M2 link-token exchange `accepted_waiting` → the callback → `answered` with `callbackSeconds`, `linkToken` and `Authorization` redacted; a refused link → `refused`, reason `ABDM-1092`, action `wait`, ABDM's message in the summary; a handler that raises → HTTP 202 to the gateway, `processing_error` holds the traceback and the API returns it; an inbound discover joined with its `m2-on-discover-care-contexts` ack through `response.requestId`, and the ack's detail names the inbound; 17 tables, paged rows, a row detail, `x_token` blanked by length; readiness: no heartbeat → blocker with the start command, heartbeat → ok, a 30-minute-old heartbeat → blocker naming the age; **0 token strings in every answer**. `m2_smoke.py` — `M2 SMOKE OK — outbound calls: 33 callbacks: 69`; `m3_smoke.py` — `M3 SMOKE OK — outbound calls: 18 callbacks: 11` (with its own superuser now); `m4_smoke.py` — `M4 SMOKE OK — NHPR calls: 58 total calls: 64`. Frontend: `tsc --noEmit` — clean; `npx eslint src` — 0 errors, 10 pre-existing warnings; i18n — 594 used keys, 0 missing, the 5 dynamic families complete, no placeholder key called without options; `npm run build` — `dist/assets/remoteEntry.js`, `developer-page-*.js` 16.5 kB, built in 1.20 s; Bruno — 90 requests parse, the 9 `dev/` paths match `urls.py`.
- [x] **Observed 2026-09-21 (URL state, findings J11):** Rithvik's screen: a tab change wrote `/abdm/developer?tab=tables&exchange=undefined&row=undefined` and both sheets opened. Cause: raviger 5.3 `serializeQuery` drops only `null`, so `undefined` became the string "undefined". Fix: `frontend/src/lib/query-params.ts` mirrors the host's `useFilters` clean rule on both sides; the explorer and the facility wizard write through it. Same pass: typed filters reach the URL 300 ms after the last key (no history entry or request per key); places push and adjustments replace; an unknown `?tab=` falls back to Exchanges; a modifier click on a tab is left to the browser; `TableRows` keyed by the table resets the cursor on Back. Proof (node, `esbuild` bundle of the helper): `raviger 5.3 wrote : ?tab=tables&exchange=undefined&row=undefined` → `cleanQuery writes : ?tab=tables`; the same URL read back → `{"tab":"tables"}`; merge `{tab, module: m2, exchange: abc} + {exchange: undefined, operation: " m2-x ", state: ""}` → `{"tab":"exchanges","module":"m2","operation":"m2-x"}`; `QUERY PARAMS CHECK OK — checks: 5`. `tsc --noEmit` clean; `eslint src` 0 errors (10 pre-existing warnings); Prettier clean; `npm run build` — `developer-page-*.js` 16.64 kB, built in 1.28 s.
- [x] **Observed 2026-09-22 (console skin, ADR-018 amendment):** Rithvik: "can all of the developer mode components have different styling. something that looks more like developers thing!" with the docs' terminal block as the reference. Built: `style/index.css` `.dark.abdm-console` (same token names, navy palette, cyan accent, `--radius` 0.375rem, 4 `--console-*` hues) after `.dark`; `@fontsource-variable/geist-mono` 5.3.0 (OFL-1.1) + careui's `--font-mono`; `dev/console.tsx` (Console, Label, Section, Prompt, Dot, Status, Kbd, CopyButton, Command, NextStep, Pairs); every dev component restyled (page status line + key caps 1–4 and `/`, log-line list, console sheet with REQUEST/ANSWER/CALLBACKS sections and `name: <N chars>` headers, editor-hued tree with guide lines, rail timeline, file-tree tables, `[ ok ]/[ !! ]/[FAIL]` readiness with copyable commands, console footer strip, admin card with `$ ABDM_DEVELOPER_MODE=true` + Copy command). Proof: built CSS holds `.care-abdm-fe-container .dark.abdm-console{color-scheme:dark;--radius:.375rem;…--background:oklch(16.5% .045 276)…--console-key:var(--color-sky-300)}`, 7 Geist Mono `@font-face` rules, `.font-mono{font-family:Geist Mono Variable,…}`, and the arbitrary classes (`shadow-[inset_2px_0_0_0_var(--primary)]`, `tracking-[0.14em]`, `text-[color:var(--console-key)]`, `border-emerald-400` for the hollow dot). The font URL in CSS is `url(./geist-mono-latin-wght-normal-XN7g48iV.woff2)` (relative; Vite's default `/assets/…` would 404 on the host origin — `experimental.renderBuiltUrl`). A 4-second `vite preview` on port 4799 (stopped after): `assets/remoteEntry.js 200 text/javascript ACAO=*`, `assets/style-B1pIW-U9.css 200 text/css ACAO=* 189999 B`, `assets/geist-mono-latin-wght-normal-XN7g48iV.woff2 200 font/woff2 ACAO=* 23128 B`. `tsc --noEmit` clean; `eslint src` 0 errors, the same 10 pre-existing warnings (the console's non-component exports moved to `dev-state.ts` for that); Prettier clean on every changed file; `npm run build` — `developer-page-*.js` 19.4 kB, 6 woff2 subsets 5.8–23.1 kB, built in 1.2 s; i18n — 591 used keys, 0 missing, 1353 total, no placeholder key called without options.
- [x] **Observed 2026-09-22 (inspector sheet, ADR-018 amendment 2):** Rithvik: "we could have like browser's inspector tab, like an general tab which shows status code, when what happened, headers and all., then separate tab for request and response." Built: `TabStrip` in `console.tsx`; the exchange sheet as General / Request / Response / Callbacks (n) / Rows (n) with `?view=` in the URL (`useCleanQueryParams`; an unknown value falls back to General), keys 1–5 inside the sheet, a reserved-height summary header, `httpReason()` (RFC 9110 phrases), query-string rows, the async reminder on Response, per-callback strips (Headers / Body / Traceback / Rows; opens on Traceback when the handler raised). 21 locale keys added, 1 orphan (`abdm_dev_headers`) and the orphan `Section` component removed. Proof: node — `VIEW CHECK OK — checks: 10` (`EXCHANGE_VIEWS` order, `isExchangeView("undefined") === false`, `httpReason(202) === "Accepted"`, `httpTone(302) === "warning"`); `tsc --noEmit` clean; `eslint src` 0 errors, the same 10 warnings; Prettier clean; `npm run build` — `exchange-sheet-*.js` 34.9 kB, built in 1.29 s; i18n — 607 used keys, 0 missing, 1373 total, no placeholder key called without options.
- [x] **Observed 2026-09-22 (colour and spacing corrections):** Rithvik's screenshots: in the sheet's General view the values of Request method, Operation, REQUEST-ID, Sent and HTTP answer, the reached timeline stops, the copy icons and the close button were black on navy; the groups touched (State → TIMING, the rail → REQUEST HEADERS); the tree's copy path/value were invisible; the tree had too much padding while the rest wanted more. Causes: (1) `color` inherits as a computed value — `SheetContent`, ghost buttons and `<dd>` set no colour and inherited the light theme's black from `.care-abdm-fe-container`; (2) `SheetBody` puts `className` on its scroller, so `grid gap-4` had 1 child; (3) Care UI's `ghost` is underlined with no base colour. Fixes: `.dark.abdm-console` sets `background-color`, `color`, `font-family` itself (the built CSS: `.care-abdm-fe-container .dark.abdm-console{color-scheme:dark;…background-color:var(--background);color:var(--foreground);font-family:var(--font-mono)…}`); the sheets own an inner `grid gap-6` wrapper; `Tool` replaces every ghost button in `dev/` (0 left); tree density 18 px rows / 12 px indent / 14 px gutter with `Tool` copy controls that appear on hover or focus; page `p-5 gap-5`, group `gap-3`, `Pairs` `gap-y-1.5 leading-5`, callback block `p-4 gap-3`, list rows `py-2.5`, readiness rows `p-4`, tables `gap-6`. `tsc --noEmit` clean; `eslint src` 0 errors, the same 10 warnings; Prettier clean; `npm run build` — `exchange-sheet-*.js` 34.9 kB, built in 1.40 s; i18n — 607 used, 0 missing, 1373 total.
- [ ] USER: set `ABDM_DEVELOPER_MODE=true` in `~/ohc.network/care/.env`, restart Care, hard-refresh. Colour and spacing: in the General view every value reads off-white (Request method `GET`, Operation, REQUEST-ID, Sent, HTTP answer, the rail's `sent` and `HTTP 200`), the copy icons and the close × are grey and turn white on hover; a clear gap separates GENERAL / TIMING / SCOPE / REQUEST HEADERS / ANSWER HEADERS; in a body tree the rows are 18 px and `path` / `value` appear grey on hover; the tree's toolbar buttons (Expand all, Raw, Copy JSON) have no underline. Inspector sheet: open an exchange → the header reads `● answered · 202 Accepted · 340 ms · POST /v0.5/…`; 5 tabs with key caps; General lists `Request URL` (copy icon), `Status code`, `Timing` (Sent, HTTP answer +ms, First callback +s), `Scope`, then the 2 header groups as `Authorization: <1448 chars>`; press `3` → Response with `← 202 Accepted` and the reminder line for a call; the address bar reads `…&exchange=<id>&view=response`; reload keeps the tab; open another exchange from Callbacks → the same tab stays; a callback whose handler raised opens on its red Traceback tab. Console skin: the explorer is 1 navy panel in Geist Mono (the browser's network tab shows `geist-mono-latin-…woff2` 200 from the plug origin); the status line reads `● abdm / developer` with the gateway host; the tabs carry key caps and `1`–`4` switch them, `/` focuses the operation filter; a row's state is a dot and a word; open an exchange → the sheet is navy too, headers read `Authorization: <1448 chars>`, the JSON tree has coloured keys and guide lines; Readiness → the worker row (when the worker is down) shows `$ cd ~/ohc.network/care && ./scripts/celery-dev.sh` with "Copy command"; `/admin/abdm` → the developer card holds a mini console; an encounter's ABDM tab ends with a thin navy strip. Screenshots: the 4 tabs, 1 sheet, the admin card, 1 footer. URL state: switch the 4 tabs — the address bar shows only `?tab=<name>` (no `exchange=` or `row=`) and no sheet opens; open an exchange → the URL gains `&exchange=<id>` → Back closes the sheet and keeps the tab; type in the operation filter → the URL changes once after the typing stops; Cmd-click a tab → a new browser tab. Then: `/admin/abdm` shows the "Developer explorer" card (on) → open it → the Exchanges tab lists today's calls (the M4 refusals with their HIS-1070 lines, the session calls); click 1 → the sheet with the timeline, header lengths and the JSON tree; Inbound → the real `on-generate-token` callbacks of 2026-09-17/18; Tables → `abha_transactions` with `x_token` as a lock badge; Readiness → 8 rows (the worker row says what the worker is doing). Then open an Encounter's ABDM tab and the patient's Demography tab: a collapsed "Developer" line at the bottom. Screenshots of the 4 tabs and 1 footer.
- [ ] 2nd pass (ADR-018 §Consequences): replay a stored callback through current code; copy as curl (redacted); a docs page link per operation; a request preview before send.

### Hardening / publish  [ ]
- Security audit checklist from `/getting-started/security-audit`.
- README, `care-package.lock`, plug_config example, `ADDITIONAL_PLUGS` example.
- App Store enrolment only on explicit approval (skill `care-apps-registry-enrolment`).

## Cross-cutting decisions
- ADR-001 Scope: Proposed. See `adr/001-scope-hip-first.md`.
- ADR-002 Secrets and encryption boundary: Accepted. See `adr/002-secrets-and-encryption-boundary.md`.
- ADR-003 Host slots: Accepted for M1 slots. Proposed for M2/M3/M4 slots. See `adr/003-host-slots.md`.
- ADR-004 Data model: Proposed. See `adr/004-data-model.md`.
- ADR-005 Callback ingress in dev and prod: Accepted. See `adr/005-dev-callback-tunnel.md`.
- ADR-006 Sync/async boundary: Proposed. See `adr/006-sync-async-boundary.md`.
- ADR-007 Pre-M4 manual facility link: Accepted. See `adr/007-pre-m4-facility-link-workaround.md`.
- ADR-008 M2 seam design: Accepted. See `adr/008-m2-seam-design.md`.
- ADR-009 Facility setup page: Accepted. See `adr/009-facility-setup-page.md`.
- ADR-010 Instance and facility scope: Accepted; `AbdmBridge` removed by ADR-011. See `adr/010-instance-vs-facility-scope.md`.
- ADR-011 Lean M2: Accepted. See `adr/011-lean-m2.md`.
- ADR-012 Error handling: Accepted. See `adr/012-error-handling.md`.
- ADR-013 Staged sharing: Accepted. See `adr/013-staged-sharing.md`.
- ADR-014 M3 Retrieve, CARE as an HIU: Accepted. See `adr/014-m3-hiu-retrieve.md`.
- ADR-015 M4 Enrol, the NHPR from CARE: Accepted. See `adr/015-m4-nhpr.md`.
- ADR-016 Facility onboarding UX: Accepted. See `adr/016-facility-onboarding-ux.md`.
- ADR-017 "My HPR ID" on the user profile: Accepted. See `adr/017-hpr-on-the-user-profile.md`.
- ADR-018 Developer mode, the audit explorer: Accepted. See `adr/018-developer-mode.md`.
