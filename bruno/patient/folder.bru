meta {
  name: patient
  seq: 7
}

docs {
  Patient-scoped routes. Set `patientId` to the `external_id` of the Care patient.
  Status and card need `can_view_patient_obj`. Link needs `can_write_patient_obj`.
}
