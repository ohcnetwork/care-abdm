meta {
  name: encounters
  seq: 9
}

docs {
  M2 HIP-initiated linking for 1 Encounter. Set `encounterId` to the Encounter `external_id`.
  The link is confirmed only by the `/v3/link/on_carecontext` callback: poll the GET after the POST.
}
