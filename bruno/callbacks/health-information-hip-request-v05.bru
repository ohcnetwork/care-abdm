meta {
  name: 15. health information request (v0.5)
  type: http
  seq: 15
}

post {
  url: {{careUrl}}/api/abdm/v0.5/health-information/hip/request
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{callbackToken}}
  REQUEST-ID: {{requestId}}
  TIMESTAMP: {{timestamp}}
  X-HIP-ID: {{hipId}}
  Content-Type: application/json
}

body:json {
  {
    "transactionId": "<TRANSACTION_ID>",
    "hiRequest": {}
  }
}

docs {
  The same request on the older path. Operation id `m2-on-health-information-request`.

  Path: `/api/abdm/v0.5/health-information/hip/request`.
}
