meta {
  name: bridge
  seq: 3
}

docs {
  The instance bridge: 1 callback URL per clientId (docs /getting-started/sandbox).
  GET reads the derived callback URL and the live gateway view. POST registers the URL (superuser only).
  The same registration runs from the CLI: `manage.py abdm_register_bridge_url`.
}
